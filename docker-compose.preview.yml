services:
  backoffice-app:
    volumes: []
    command: sh -lc "npm run build && npm run preview -- --host 0.0.0.0 --port 3000"

  client-app:
    volumes: []
    command: sh -lc "npm run build && npm run preview -- --host 0.0.0.0 --port 3003"
