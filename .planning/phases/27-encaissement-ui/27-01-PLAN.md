---
phase: 27-encaissement-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/shared/types/paiements.ts
  - app/frontend/backoffice/src/components/salle/PaymentModal.tsx
  - app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx
autonomous: true
requirements: [ENC-01, ENC-03]
must_haves:
  truths:
    - "Staff can open a payment modal from the Table Map for occupied tables"
    - "The modal displays the current order summary and montant_restant"
    - "Staff can generate a QR code for client payment"
    - "Staff can manually mark an order as paid (Espèces/Carte)"
  artifacts:
    - path: "app/frontend/backoffice/src/components/salle/PaymentModal.tsx"
      provides: "Staff-facing payment management interface"
    - path: "app/frontend/shared/types/paiements.ts"
      provides: "Payment-related type definitions"
  key_links:
    - from: "app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx"
      to: "app/frontend/backoffice/src/components/salle/PaymentModal.tsx"
      via: "State-controlled modal rendering"
---

<objective>
Implement the Staff-facing Payment Modal in the Backoffice app, allowing servers to view order totals, generate payment QR codes, and record manual payments.

Purpose: Enable on-site payment resolution and self-service initiation.
Output: Integrated `PaymentModal` component and updated `MapView`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-encaissement-ui/27-CONTEXT.md
@.planning/phases/27-encaissement-ui/27-RESEARCH.md
@.planning/phases/27-encaissement-ui/27-UI-SPEC.md
@app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Payment Types & Directory Setup</name>
  <files>app/frontend/shared/types/paiements.ts</files>
  <action>
    - Create `app/frontend/backoffice/src/components/salle/` directory.
    - Define `PaiementStatus`, `PaiementMethod`, and `PaymentSession` types in `app/frontend/shared/types/paiements.ts`.
    - Include types for QR token response (`{ token: string }`).
  </action>
  <verify>
    <automated>test -f app/frontend/shared/types/paiements.ts</automated>
  </verify>
  <done>Types defined and directory created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement PaymentModal Component</name>
  <files>app/frontend/backoffice/src/components/salle/PaymentModal.tsx</files>
  <action>
    - Create `PaymentModal` using `@shared/ui/Modal`.
    - Display order summary (lines, prices) and `montant_restant`.
    - Add "Générer QR" button that calls `GET /api/tables/{id}/qr/` and displays the QR (use `qrcode.react`).
    - Add "Paiement Manuel" section with [Espèces] and [Carte] buttons calling `POST /api/paiements/` with `{"commande": id, "mode": "ESPECES|CARTE", "montant": montant_restant}`.
    - Handle loading and error states for API calls.
  </action>
  <verify>
    <automated>npm run build --prefix app/frontend/backoffice</automated>
  </verify>
  <done>Component implemented with QR generation and manual payment logic.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Modal to MapView</name>
  <files>app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx</files>
  <action>
    - Import `PaymentModal` into `MapView.tsx`.
    - Add a "Régler l'addition" button to the `TableInfoPanel` (or equivalent sidebar section) when a table is selected and has an active order.
    - Manage modal visibility state in `MapView`.
    - Ensure modal receives the correct table/order context.
  </action>
  <verify>
    <automated>grep -q "PaymentModal" app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx</automated>
  </verify>
  <done>Modal integrated into the Table Map workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Staff UI -> Backend | Manual payment records cross here |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | Manual Payment Request | mitigate | Backend validates staff permissions and order ownership (Phase 26 logic) |
| T-27-02 | Information Disclosure | QR Token | mitigate | Tokens are signed and time-limited (Phase 26 logic) |
</threat_model>

<verification>
1. Select an occupied table in the Table Map.
2. Click "Régler l'addition".
3. Verify order total and items are correct.
4. Click "Générer QR" and verify QR code appears.
5. Click "Espèces" and verify the order is marked as paid and table is freed (via backend signal).
</verification>

<success_criteria>
- Payment modal is functional and accurately reflects backend order state.
- QR generation works correctly.
- Manual payments successfully transition orders to `PAYEE`.
</success_criteria>

<output>
After completion, create `.planning/phases/27-encaissement-ui/27-01-SUMMARY.md`
</output>
