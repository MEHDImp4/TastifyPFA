---
phase: 27-encaissement-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/portail/src/App.tsx
  - app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx
  - app/frontend/portail/src/components/payment/SplitSelector.tsx
autonomous: true
requirements: [ENC-02, ENC-03]
must_haves:
  truths:
    - "Clients can access a payment page via a tokenized URL"
    - "Clients can choose between Full Payment, Equal Split, or Item-based Split"
    - "Clients can see exactly how much they are about to pay"
    - "Payment 'confirmation' triggers a success state in the UI"
  artifacts:
    - path: "app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx"
      provides: "Public client-side payment landing page"
    - path: "app/frontend/portail/src/components/payment/SplitSelector.tsx"
      provides: "UI for choosing split strategy"
  key_links:
    - from: "app/frontend/portail/src/App.tsx"
      to: "app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx"
      via: "React Router route definition (/pay/:token)"
---

<objective>
Implement the Client-facing Payment Landing Page in the Portail app, allowing customers to resolve their table session, choose a split method (Total, Equal, or Item), and complete the payment process.

Purpose: Provide self-service checkout for customers.
Output: Integrated `PaymentLandingPage` and split selection components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-encaissement-ui/27-CONTEXT.md
@.planning/phases/27-encaissement-ui/27-RESEARCH.md
@.planning/phases/27-encaissement-ui/27-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Route and Landing Page Scaffold</name>
  <files>app/frontend/portail/src/App.tsx, app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx</files>
  <action>
    - Create `app/frontend/portail/src/pages/Payment/` directory.
    - Add `/pay/:token` route to `App.tsx` pointing to `PaymentLandingPage`.
    - Implement token resolution in `PaymentLandingPage` (call `POST /api/paiements/session/resolve/` with the token from URL).
    - Handle invalid/expired token states with a friendly error message.
  </action>
  <verify>
    <automated>npm run build --prefix app/frontend/portail</automated>
  </verify>
  <done>Route registered and session resolution logic implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Split Selector & Configuration</name>
  <files>app/frontend/portail/src/components/payment/SplitSelector.tsx, app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx</files>
  <action>
    - Create `SplitSelector` component displaying 3 cards: "Tout Payer", "Partage Égal", "Par Article".
    - Implement Equal Split config: A guest counter that calls `POST /api/paiements/session/equal-split/` to preview the share amount.
    - Implement Item Split config: A checkbox list of unpaid order items that calls `POST /api/paiements/session/item-split/` to calculate the total for selected items.
    - Use `ECO-FRESH` colors and standard cards from `@shared/ui`.
  </action>
  <verify>
    <automated>grep -q "SplitSelector" app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx</automated>
  </verify>
  <done>Split selection UI and configuration logic implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Payment Confirmation Simulation</name>
  <files>app/frontend/portail/src/pages/Payment/PaymentLandingPage.tsx</files>
  <action>
    - Add a "Confirmer le paiement" button that appears after a split selection and amount calculation.
    - When clicked, simulate an external payment success (in a real app, this would redirect to a gateway).
    - Call the final payment creation endpoint (e.g., `POST /api/paiements/` with the calculated payload).
    - Show a success "Thank You" screen with table release notification.
  </action>
  <verify>
    <automated>npm run build --prefix app/frontend/portail</automated>
  </verify>
  <done>Payment flow completed with success state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client Browser -> API | Tokenized session resolution and split requests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Tampering | Split Amount Calculation | mitigate | Backend re-calculates all amounts; frontend only provides selection IDs. |
| T-27-04 | Denial of Service | Token Resolution | mitigate | Rate limiting on public resolve endpoints (Phase 26 logic). |
</threat_model>

<verification>
1. Navigate to `/pay/{valid_token}`.
2. Verify table and order details load.
3. Select "Partage Égal" and change guest count; verify amount updates.
4. Select specific items and verify total updates.
5. Click "Confirmer" and verify success screen.
</verification>

<success_criteria>
- Clients can resolve sessions and choose split methods independently.
- Split calculations match backend logic.
- UI handles loading/error states gracefully.
</success_criteria>

<output>
After completion, create `.planning/phases/27-encaissement-ui/27-02-SUMMARY.md`
</output>
