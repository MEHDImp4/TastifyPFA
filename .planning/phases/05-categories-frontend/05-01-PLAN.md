---
phase: 05-categories-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/back-office/package.json
  - frontend/back-office/src/App.tsx
  - frontend/back-office/src/App.test.tsx
  - frontend/back-office/src/components/layout/Sidebar.tsx
  - frontend/back-office/src/components/layout/AppShell.tsx
  - frontend/back-office/src/components/layout/AppShell.test.tsx
  - frontend/back-office/src/pages/Categories/index.tsx
autonomous: true
requirements: [PHASE-05-1]
must_haves:
  truths:
    - User can log in and see the AppShell layout
    - AppShell redirects unauthenticated users to the Login component
    - Sidebar has correct sections and highlighted active route
    - Navigation to /categories renders the CategoriesPage placeholder
  artifacts:
    - path: frontend/back-office/src/components/layout/AppShell.tsx
      provides: Layout wrapper with sidebar and outlet
    - path: frontend/back-office/src/App.tsx
      provides: React Router setup
  key_links:
    - from: frontend/back-office/src/App.tsx
      to: react-router-dom
      via: BrowserRouter
---

<objective>
Establish the AppShell layout and routing for the back-office SPA, adhering strictly to TDD.
Purpose: Create a scalable, tested foundation for all back-office routes (categories, plats, tables, etc.).
Output: Installed router, AppShell layout with Sidebar, basic routing, and corresponding unit/integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-categories-frontend/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router</name>
  <files>frontend/back-office/package.json</files>
  <read_first>.planning/phases/05-categories-frontend/05-RESEARCH.md</read_first>
  <action>
    Run `npm install react-router-dom@^6` inside the `frontend/back-office` directory. Also ensure testing dependencies like `vitest` and `@testing-library/react` are present or install them as needed.
  </action>
  <verify>
    <automated>npm run build --prefix frontend/back-office</automated>
  </verify>
  <acceptance_criteria>
    - `react-router-dom` is present in package.json dependencies.
  </acceptance_criteria>
  <done>Package installed successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AppShell and Sidebar Layout</name>
  <files>frontend/back-office/src/components/layout/Sidebar.tsx, frontend/back-office/src/components/layout/AppShell.tsx, frontend/back-office/src/components/layout/AppShell.test.tsx</files>
  <read_first>frontend/_shared/theme.css, frontend/_shared/auth/useAuthStore.ts</read_first>
  <behavior>
    - Test 1: Sidebar renders navigation links correctly
    - Test 2: AppShell redirects unauthenticated users to /login
    - Test 3: AppShell renders Sidebar and Outlet for authenticated users
  </behavior>
  <action>
    Create `frontend/back-office/src/components/layout/Sidebar.tsx` displaying a static ≈220px column using Tailwind `w-56 h-screen fixed inset-y-0 left-0 bg-surface shadow-lg text-white`. Import `lucide-react` icons (e.g., LayoutGrid, List, LayoutDashboard). Include navigation entries: Catégories (`/categories`), Plats, Tables, Stock, HR, Dashboard (use placeholder `#` links for unbuilt routes). Highlight the active route with a teal text/accent using `react-router-dom`'s `<NavLink>`.
    Create `frontend/back-office/src/components/layout/AppShell.tsx` applying a flex row layout: render `<Sidebar />` on the left, and a main content area with `<Outlet />` on the right (`ml-56 flex-1 bg-background min-h-screen p-8`). Import `useAuthStore` and check `isAuthenticated`. If false, execute `return <Navigate to="/login" replace />;`.
  </action>
  <verify>
    <automated>npm test run --prefix frontend/back-office</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar renders navigation links correctly with distinct background colors.
    - AppShell blocks unauthenticated users from seeing the `<Outlet />`.
    - Tests pass verifying authentication gating.
  </acceptance_criteria>
  <done>Layout components created and guarded by authentication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Setup Application Routing</name>
  <files>frontend/back-office/src/App.tsx, frontend/back-office/src/pages/Categories/index.tsx, frontend/back-office/src/App.test.tsx</files>
  <read_first>frontend/_shared/auth/Login.tsx, frontend/_shared/auth/useAuthStore.ts</read_first>
  <behavior>
    - Test 1: App renders Router with AppShell and routes
    - Test 2: Unauthenticated access redirects to /login
    - Test 3: Authenticated access to / redirects to /categories
  </behavior>
  <action>
    Create `frontend/back-office/src/pages/Categories/index.tsx` exporting a simple React component that returns `<h1 className="text-3xl text-teal-400 font-bold">Catégories</h1>` as a placeholder.
    Refactor `frontend/back-office/src/App.tsx` to return `<BrowserRouter>`. Define a top-level `<Routes>` block:
    - Define a public `<Route path="/login" element={<Login />} />` (importing from `../../_shared/auth/Login`).
    - Define `<Route element={<AppShell />}>` wrapping authenticated routes:
      - `<Route index element={<Navigate to="/categories" replace />} />`
      - `<Route path="/categories" element={<CategoriesPage />} />`
  </action>
  <verify>
    <automated>npm test run --prefix frontend/back-office</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx utilizes React Router components structure.
    - User arriving at `/` gets redirected to `/categories`.
    - User at `/categories` sees the AppShell + "Catégories" heading.
    - Tests verify routing behaviors correctly.
  </acceptance_criteria>
  <done>Routing successfully deployed connecting the authentication shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→AppShell | Ensures only authenticated users can access the main layouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Elevation | AppShell / App | mitigate | Gate access inside `<AppShell>` via Zustand `isAuthenticated` and redirect to Login route. |
</threat_model>

<verification>
Check that running `npm run dev` serves a functional React app where unauthenticated access shows the shared Login form, and logging in displays the AppShell with Sidebar. Tests must pass.
</verification>

<success_criteria>
React Router is installed, AppShell uses correct theme colors, sidebar renders icons and links, and authentication gating works correctly on the frontend via React Router. Unit tests cover layout and routing logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-categories-frontend/05-01-SUMMARY.md`
</output>
