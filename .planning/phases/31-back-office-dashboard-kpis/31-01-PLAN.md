---
phase: 31-back-office-dashboard-kpis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/backend/apps/analytics/views.py, app/backend/apps/analytics/urls.py, app/backend/apps/analytics/apps.py, app/backend/tastify_backend/urls.py, app/backend/apps/analytics/signals.py]
autonomous: true
requirements: [D-31-01, D-31-02]

must_haves:
  truths:
    - "Backend provides a single endpoint for all dashboard metrics"
    - "Revenue calculations correctly filter for 'COMPLETE' payments today"
    - "Average prep time handles duration calculations in Django"
  artifacts:
    - path: "app/backend/apps/analytics/views.py"
      provides: "Dashboard KPI aggregation logic"
    - path: "app/backend/apps/analytics/signals.py"
      provides: "Real-time WebSocket event triggers"
  key_links:
    - from: "app/backend/apps/analytics/views.py"
      to: "app/backend/apps/paiements/models.py"
      via: "Paiement.objects.completed()"
---

<objective>
Implement the backend aggregation logic for the dashboard KPIs and historical trends.

Purpose: Provide a performant and consolidated API for the manager dashboard.
Output: /api/analytics/dashboard/ endpoint and WebSocket signals.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/31-back-office-dashboard-kpis/31-CONTEXT.md
@.planning/phases/31-back-office-dashboard-kpis/31-RESEARCH.md
@app/backend/apps/paiements/models.py
@app/backend/apps/commandes/models.py
@app/backend/apps/tables/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Analytics App</name>
  <files>app/backend/apps/analytics/apps.py, app/backend/apps/analytics/__init__.py, app/backend/tastify_backend/settings.py</files>
  <action>
    Create the 'analytics' app in app/backend/apps/.
    Register it in INSTALLED_APPS in tastify_backend/settings.py.
    Ensure it follows the standard app structure of the project.
  </action>
  <verify>
    <automated>python app/backend/manage.py check</automated>
  </verify>
  <done>Analytics app registered and recognized by Django.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dashboard API</name>
  <files>app/backend/apps/analytics/views.py, app/backend/apps/analytics/urls.py, app/backend/tastify_backend/urls.py</files>
  <action>
    Create a DashboardViewSet (or APIView) in analytics/views.py.
    Implement the following metrics (per D-31-01, D-31-02):
    - today_revenue: Sum of Paiement.completed() for today.
    - active_tables: Count of Table.Statut.OCCUPEE.
    - pending_orders: Count of Commande.Statut.EN_CUISINE or EN_COURS.
    - avg_prep_time: Avg duration (updated_at - heure_lancement) for served items today.
    - revenue_7_days: Daily revenue for the last 7 days.
    - top_dishes: Top 5 dishes by quantity.
    Expose at /api/analytics/dashboard/.
    Ensure 'Ardoise' theme friendly naming if necessary (e.g. camelCase for frontend).
  </action>
  <verify>
    <automated>curl -X GET http://localhost:8000/api/analytics/dashboard/ -H "Authorization: Bearer [TOKEN]"</automated>
  </verify>
  <done>Endpoint returns correct JSON payload with all required KPIs.</done>
</task>

<task type="auto">
  <name>Task 3: Real-time Signal Triggers</name>
  <files>app/backend/apps/analytics/signals.py, app/backend/apps/analytics/apps.py</files>
  <action>
    Implement signals (post_save) on Paiement, Table, and Commande.
    When a change occurs that affects KPIs (e.g. payment completed, table status changed):
    Trigger a 'dashboard_update' event to the STAFF_GROUP via channel_layer.
    Reference D-31-03.
  </action>
  <verify>
    <automated>python app/backend/manage.py test apps/analytics/tests</automated>
  </verify>
  <done>Signals correctly broadcast dashboard update events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | External request to analytics data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Information Disclosure | Dashboard API | mitigate | Restrict access to GERANT role only using IsAuthenticated and custom RolePermission. |
</threat_model>

<verification>
Check /api/analytics/dashboard/ returns valid data.
Verify signals trigger without errors during normal operations.
</verification>

<success_criteria>
Backend provides all necessary data for the dashboard in a single request.
Real-time events are dispatched when critical data changes.
</success_criteria>

<output>
After completion, create .planning/phases/31-back-office-dashboard-kpis/31-01-SUMMARY.md
</output>
