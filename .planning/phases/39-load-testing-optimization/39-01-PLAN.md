---
phase: 39
plan: 01
slug: asgi-production-config
status: planned
---

# Plan 39-01: ASGI Production Configuration

## Goal
Switch the backend from `runserver` to a production-grade ASGI server (Daphne).

## Tasks
1.  **Daphne Setup**:
    - [ ] Update `backend/Dockerfile` to install `daphne` (already in requirements).
    - [ ] Update `docker-compose.yml` backend command to: `daphne -b 0.0.0.0 -p 8000 tastify_backend.asgi:application`.
2.  **Environment Tuning**:
    - [ ] Set `CONN_MAX_AGE` in settings to reuse DB connections.
    - [ ] Ensure `DEBUG=False` for load testing.
3.  **Static Serving**:
    - [ ] Enable `whitenoise` for serving statics through Daphne (already in requirements).
    - [ ] Update `settings.py` middleware for Whitenoise.

## Verification
- [ ] Backend starts successfully with `daphne`.
- [ ] API and WebSockets still functional.
