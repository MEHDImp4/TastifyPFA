---
phase: 03
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  frontend/back-office/vite.config.ts,
  frontend/salle/vite.config.ts,
  frontend/kds/vite.config.ts,
  frontend/portail-client/vite.config.ts,
  frontend/_shared/auth/useAuthStore.ts,
  frontend/_shared/auth/axiosInstance.ts,
  frontend/_shared/auth/Login.tsx
]
autonomous: true
requirements: [AUTH-04, AUTH-05, AUTH-06]
must_haves:
  truths:
    - "Vite apps can import from @shared/ alias"
    - "useAuthStore manages accessToken and user state"
    - "axiosInstance automatically refreshes token on 401"
    - "Login component provides a clean UI for credentials entry"
  artifacts:
    - path: "frontend/_shared/auth/useAuthStore.ts"
      provides: "Global auth state management (Zustand)"
    - path: "frontend/_shared/auth/axiosInstance.ts"
      provides: "API client with refresh interceptor"
    - path: "frontend/_shared/auth/Login.tsx"
      provides: "Shared Login UI component"
  key_links:
    - from: "frontend/_shared/auth/Login.tsx"
      to: "frontend/_shared/auth/useAuthStore.ts"
      via: "hook usage"
    - from: "frontend/_shared/auth/axiosInstance.ts"
      to: "frontend/_shared/auth/useAuthStore.ts"
      via: "accessing/updating tokens"
---

<objective>
Setup the shared frontend infrastructure for authentication, including the Zustand state store, the Axios interceptor logic for automatic token refreshing, and a reusable Login component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-auth-api-login/03-CONTEXT.md
@.planning/phases/03-auth-api-login/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vite Aliases for Shared Components</name>
  <files>frontend/back-office/vite.config.ts, frontend/salle/vite.config.ts, frontend/kds/vite.config.ts, frontend/portail-client/vite.config.ts</files>
  <action>
    - Add `resolve.alias` to each `vite.config.ts` mapping `@shared` to the `frontend/_shared` directory.
    - Use `path.resolve(__dirname, '../_shared')` for the mapping.
    - Ensure `path` is imported in `vite.config.ts`.
  </action>
  <verify>
    <automated>ls frontend/_shared/theme.css</automated>
  </verify>
  <done>Vite aliases configured for all 4 SPAs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Auth Store and Axios Interceptors</name>
  <files>frontend/_shared/auth/useAuthStore.ts, frontend/_shared/auth/axiosInstance.ts</files>
  <behavior>
    - useAuthStore: stores `user`, `role`, `accessToken`, `isAuthenticated`. Persist to sessionStorage if needed (or keep in memory per design).
    - axiosInstance:
      - Request interceptor: attaches Bearer token if available.
      - Response interceptor: if 401, queues requests, calls `/api/users/refresh/`, updates store, and retries queued requests.
  </behavior>
  <action>
    - Create `frontend/_shared/auth/useAuthStore.ts` using Zustand.
    - Create `frontend/_shared/auth/axiosInstance.ts` with robust interceptor logic as per RESEARCH.md.
  </action>
  <verify>
    <automated>grep -r "axios.create" frontend/_shared/auth/</automated>
  </verify>
  <done>Auth store and API client implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Create Shared Login Component</name>
  <files>frontend/_shared/auth/Login.tsx</files>
  <action>
    - Create a modern, responsive Login component in `frontend/_shared/auth/Login.tsx`.
    - Features:
      - Email/Username and Password fields.
      - Show/Hide password toggle.
      - Error handling (invalid credentials).
      - Loading state during submission.
      - Success callback (redirection handled by consumer).
    - Use Tailwind CSS for styling (Eco-Fresh palette).
  </action>
  <verify>
    <automated>grep "export default Login" frontend/_shared/auth/Login.tsx</automated>
  </verify>
  <done>Shared Login UI component created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| JS Memory -> LocalStorage | Prohibited for tokens. Access token stays in memory (Zustand). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | Access Token | Mitigate | Stored in JS memory (Zustand), not LocalStorage/Cookies. |
| T-03-05 | Denial of Service | Token Refresh | Mitigate | Interceptor queueing prevents multiple simultaneous refresh requests. |
</threat_model>

<success_criteria>
1. All Vite configs updated.
2. `useAuthStore` and `axiosInstance` exist and contain specified logic.
3. `Login.tsx` component is exported and stylistically consistent.
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-api-login/03-02-SUMMARY.md`
</output>
