---
phase: 34
plan: 01
slug: kds-advanced-logic
status: planned
---

# Plan 34-01: KDS Advanced Logic & API

## Goal
Implement the backend logic for UC19 (Modification) and UC20_bis (Rupture).

## Tasks
1.  **Menu API Enhancement**:
    - [ ] Ensure `PlatViewSet` allows partial update of `est_disponible` by `CUISINIER`.
2.  **Order Modification Logic**:
    - [ ] Implement `patch /api/commandes/lignes/{id}/` to allow `statut = ANNULE` if currently `EN_ATTENTE`.
    - [ ] Add backend validation: prevent cancellation if `statut` is `EN_PREPARATION` without kitchen code (or just strict policy).
3.  **Signals & WebSockets**:
    - [ ] In `menu.signals`, trigger `broadcast_menu_update` when `est_disponible` changes.
    - [ ] In `commandes.signals`, ensure `ANNULE` status updates KDS UI in real-time.
4.  **Tests**:
    - [ ] Test: Cuisinier marks plat as unavailable -> success.
    - [ ] Test: Serveur cancels pending line -> success.
    - [ ] Test: Serveur tries to cancel 'EN_PREPARATION' line -> error 403.

## Verification
- [ ] Pytest suite for `menu` and `commandes` apps passes.
