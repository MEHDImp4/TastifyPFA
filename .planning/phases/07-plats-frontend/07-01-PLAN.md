---
phase: 07-plats-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/back-office/src/App.tsx
  - frontend/back-office/src/App.test.tsx
  - frontend/back-office/src/components/layout/Sidebar.tsx
  - frontend/back-office/src/pages/Plats/index.tsx
  - frontend/back-office/src/pages/Plats/index.test.tsx
  - frontend/back-office/src/pages/Plats/types.ts
  - frontend/back-office/src/pages/Plats/useResponsiveListView.ts
  - frontend/back-office/src/pages/Plats/useResponsiveListView.test.ts
autonomous: true
requirements:
  - PHASE-07-1
must_haves:
  truths:
    - The back-office sidebar links to `/plats`
    - Visiting `/plats` renders the plats page inside the existing AppShell
    - The page owns category filter state with default `all`
    - The page can request dishes with optional `?categorie=<id>` filtering
    - A dedicated responsive seam determines desktop table vs mobile cards
---

<objective>
Establish the Phase 7 frontend foundation before any detailed dish UI is built.
Purpose: wire the new page into the existing back-office shell, define typed data contracts, and centralize the category filter plus responsive decision seam.
Output: reachable `/plats` route, active sidebar entry, typed plats page scaffold, and tests covering the new foundation.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-plats-frontend/07-CONTEXT.md
@.planning/phases/07-plats-frontend/07-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire route and sidebar navigation for plats</name>
  <files>
    frontend/back-office/src/App.tsx
    frontend/back-office/src/App.test.tsx
    frontend/back-office/src/components/layout/Sidebar.tsx
  </files>
  <behavior>
    - Authenticated navigation to `/plats` renders the plats page inside AppShell
    - Sidebar highlights `Plats` as an active route entry
    - Existing categories routing remains intact
  </behavior>
  <action>
    Replace the placeholder `Plats` navigation target in `Sidebar.tsx` with `/plats`.
    Register a real `/plats` route in `App.tsx` using the existing router structure and AppShell nesting.
    Extend `App.test.tsx` so route coverage includes the new plats page render path.
  </action>
  <verify>
    <automated>npm test run --prefix frontend/back-office -- src/App.test.tsx</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create typed contracts and responsive seam</name>
  <files>
    frontend/back-office/src/pages/Plats/types.ts
    frontend/back-office/src/pages/Plats/useResponsiveListView.ts
    frontend/back-office/src/pages/Plats/useResponsiveListView.test.ts
  </files>
  <behavior>
    - Shared plats/category types match the live API contract
    - The responsive hook returns desktop vs mobile list mode based on a breakpoint seam
    - The seam is directly testable without coupling to full page rendering
  </behavior>
  <action>
    Add a `types.ts` module containing typed category and plat interfaces aligned with the backend API.
    Create a `useResponsiveListView` hook or seam that returns the current list presentation mode and can be verified independently in tests.
  </action>
  <verify>
    <automated>npm test run --prefix frontend/back-office -- src/pages/Plats/useResponsiveListView.test.ts</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build page scaffold with category filter state and data loading</name>
  <files>
    frontend/back-office/src/pages/Plats/index.tsx
    frontend/back-office/src/pages/Plats/index.test.tsx
  </files>
  <behavior>
    - Page loads categories and dishes through the shared axios client
    - Category filter defaults to all categories
    - Selecting a category updates the dishes request scope
    - Page renders a neutral scaffold for later desktop/mobile list components
  </behavior>
  <action>
    Create the plats page container with a primary category filter, local fetch state, and data requests against `/api/categories/` and `/api/plats/` with optional `?categorie=<id>`.
  </action>
  <verify>
    <automated>npm test run --prefix frontend/back-office -- src/pages/Plats/index.test.tsx</automated>
  </verify>
</task>

</tasks>

<success_criteria>
The plats route is live, the sidebar points to it, typed frontend contracts exist for the dishes API, category filtering defaults to all categories, and the page has a tested responsive seam ready for desktop/mobile rendering.
</success_criteria>
