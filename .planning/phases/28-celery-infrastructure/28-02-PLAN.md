---
phase: 28-celery-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: [app/backend/tastify_backend/settings/base.py, app/backend/tastify_backend/api_router.py]
autonomous: true
requirements: [CHECKLIST-01, CHECKLIST-02]
must_haves:
  truths:
    - "Checklist templates can be created with associated tasks"
    - "Checklist executions can be recorded for specific dates"
    - "API allows fetching current day's checklist executions"
  artifacts:
    - path: "app/backend/apps/checklists/models.py"
      provides: "Checklist, TaskChecklist, ChecklistExecution, ChecklistItemResponse"
    - path: "app/backend/apps/checklists/serializers.py"
      provides: "Checklist logic serialization"
---

<objective>
Implement the core data domain for the restaurant's daily checklists, enabling management to define procedures and staff to record their completion.

Purpose: Formalize opening/closing procedures for operational consistency.
Output: Checklist Django application with REST API endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-celery-infrastructure/28-RESEARCH.md
@app/backend/tastify_backend/api_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Checklists App and Models</name>
  <files>app/backend/apps/checklists/models.py, app/backend/apps/checklists/apps.py</files>
  <action>
    - Create `apps.checklists` using `python manage.py startapp checklists` (in docker).
    - Implement models as per RESEARCH.md:
        - `Checklist`: Template for a list (e.g., "Ouverture Cuisine").
        - `TaskChecklist`: Individual items in a template (e.g., "Allumer les fours").
        - `ChecklistExecution`: An instance of a checklist for a specific date.
        - `ChecklistItemResponse`: The status (done/not done) of a specific task in an execution.
    - Add `apps.checklists` to `INSTALLED_APPS` in `settings/base.py`.
  </action>
  <verify>
    <automated>python app/backend/manage.py makemigrations checklists --check</automated>
  </verify>
  <done>Checklist models implemented and migrations ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement REST API for Checklists</name>
  <files>app/backend/apps/checklists/serializers.py, app/backend/apps/checklists/views.py, app/backend/tastify_backend/api_router.py</files>
  <action>
    - Create serializers for all Checklist models.
    - Implement `ChecklistExecutionViewSet` with support for:
        - Listing executions for the current date.
        - Updating `ChecklistItemResponse` (mark as done).
    - Register the new ViewSet in `api_router.py`.
  </action>
  <verify>
    <automated>python app/backend/manage.py test apps.checklists</automated>
  </verify>
  <done>REST API for checklists is functional and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Staff -> API | RBAC required (GERANT to manage templates, staff to execute) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | S | API | mitigate | Restrict template management (POST/DELETE on Checklist) to GERANT role. |
| T-28-04 | I | Execution | mitigate | Use `unique_together` on ChecklistExecution (checklist, date) to prevent duplicate daily instances. |
</threat_model>

<verification>
Run migrations and use `curl` or Postman to create a Checklist template and a ChecklistExecution instance.
</verification>

<success_criteria>
1. `GET /api/checklists/executions/` returns checklist items for the day.
2. `PATCH /api/checklists/responses/{id}/` successfully updates completion status.
</success_criteria>

<output>
After completion, create `.planning/phases/28-celery-infrastructure/28-02-SUMMARY.md`
</output>
