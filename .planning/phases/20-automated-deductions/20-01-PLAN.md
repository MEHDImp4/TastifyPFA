---
phase: 20-automated-deductions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/backend/apps/stock/services.py",
  "app/backend/apps/stock/tests/test_services.py",
  "app/backend/apps/commandes/tasks.py",
  "app/backend/apps/commandes/tests/test_stock_integration.py"
]
autonomous: true
requirements: [REQ-20-01, REQ-20-02, REQ-20-03]

must_haves:
  truths:
    - "Ingredient stock levels decrease automatically when a kitchen item is launched (EN_PREPARATION)."
    - "Concurrent deductions are handled safely without race conditions."
    - "Low stock alerts are broadcast to staff when thresholds are crossed during automated deduction."
  artifacts:
    - path: "app/backend/apps/stock/services.py"
      provides: "Atomic stock deduction logic"
    - path: "app/backend/apps/commandes/tasks.py"
      provides: "Deduction trigger in JIT task"
  key_links:
    - from: "app/backend/apps/commandes/tasks.py"
      to: "app/backend/apps/stock/services.py"
      via: "Service call in launch_item_task"
---

<objective>
Implement automated stock deduction triggered by the JIT orchestration system.
When an item is launched for preparation, its constituent ingredients (defined in the plat's recipe) must be subtracted from the inventory atomically.

Purpose: Maintain real-time inventory accuracy and trigger low-stock alerts.
Output: StockService, integrated task logic, and comprehensive test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-automated-deductions/20-RESEARCH.md
@app/backend/apps/stock/models.py
@app/backend/apps/stock/signals.py
@app/backend/apps/commandes/tasks.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement StockService and Unit Tests</name>
  <files>app/backend/apps/stock/services.py, app/backend/apps/stock/tests/test_services.py</files>
  <behavior>
    - Test 1: Given a Plat with multiple Ingredients, deduct_ingredients_for_plat subtracts (quantite_requise * multiplier) from each ingredient.
    - Test 2: Concurrent calls to deduct from the same ingredient handle locking correctly (no lost updates).
    - Test 3: Deduction triggers existing post_save signals for low stock alerts.
    - Test 4: Deadlock prevention: Ingredients are locked in a deterministic order (by ID).
  </behavior>
  <action>
    1. Create `app/backend/apps/stock/services.py`.
    2. Implement `StockService.deduct_ingredients_for_plat(plat, quantity)`:
       - Use `@transaction.atomic`.
       - Fetch `PlatIngredient` for the plat, ordered by `ingredient_id` (deadlock prevention).
       - Use `select_for_update()` on each `Ingredient` row.
       - Update `stock_actuel` and call `.save()` to ensure signals fire.
    3. Create `app/backend/apps/stock/tests/test_services.py` with the described behaviors.
  </action>
  <verify>
    <automated>pytest app/backend/apps/stock/tests/test_services.py</automated>
  </verify>
  <done>StockService is functional and passes all unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Stock Deduction into Celery Task</name>
  <files>app/backend/apps/commandes/tasks.py, app/backend/apps/commandes/tests/test_stock_integration.py</files>
  <behavior>
    - Test 1: When launch_item_task is called, the corresponding ingredients are deducted from stock.
    - Test 2: If the task is skipped (idempotency), no deduction occurs.
    - Test 3: If deduction fails (e.g. database error), the status update is rolled back (ensure atomic task operation).
  </behavior>
  <action>
    1. Modify `app/backend/apps/commandes/tasks.py`:
       - Import `StockService` from `apps.stock.services`.
       - Wrap the status update and service call in a `transaction.atomic` block within `launch_item_task`.
       - Call `StockService.deduct_ingredients_for_plat(line.plat, line.quantite)` immediately after the `update()` or change `update()` to instance save to maintain atomicity and signal flow.
    2. Create `app/backend/apps/commandes/tests/test_stock_integration.py` to verify the end-to-end flow from task trigger to stock reduction.
  </action>
  <verify>
    <automated>pytest app/backend/apps/commandes/tests/test_stock_integration.py</automated>
  </verify>
  <done>launch_item_task correctly triggers automated stock deduction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service Layer | Internal backend calls; assumes valid Plat and Quantity data passed from Commandes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | StockService | mitigate | Use `select_for_update()` and `transaction.atomic` to prevent race conditions (double deduction or lost updates). |
| T-20-02 | Denial of Service | StockService | mitigate | Order ingredient locking by ID to prevent deadlocks when multiple processes deduct shared ingredients. |
</threat_model>

<verification>
1. Run `pytest app/backend/apps/stock/tests/test_services.py` to verify core logic.
2. Run `pytest app/backend/apps/commandes/tests/test_stock_integration.py` to verify task integration.
3. Manually verify (if possible via CLI) that `Ingredient.stock_actuel` is reduced after a simulated order line launch.
</verification>

<success_criteria>
- Stock is automatically deducted when an item is launched.
- Deduction is atomic and race-condition-safe.
- Low stock alerts fire correctly via existing WebSocket signals.
- No deadlocks occur under concurrent load.
</success_criteria>

<output>
After completion, create `.planning/phases/20-automated-deductions/20-01-SUMMARY.md`
</output>
