---
phase: 02-user-model-rbac
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/apps/users/permissions.py
  - backend/apps/users/tests/test_permissions.py
autonomous: true
requirements:
  - REQ-02-3
  - REQ-02-4
must_haves:
  truths:
    - DRF endpoints can restrict access based on user role.
    - GERANT inherits staff privileges.
  artifacts:
    - path: backend/apps/users/permissions.py
      provides: DRF BasePermission classes
  key_links:
    - from: backend/apps/users/permissions.py
      to: backend/apps/users/models.py
      via: Utilisateur.Role choices
---

<objective>
Implement Role-Based Access Control (RBAC) permission classes for Django REST Framework.

Purpose: Establish the security layer that will protect future API endpoints by strictly evaluating the incoming user's role against the required access level.
Output: Reusable DRF permission classes `IsGerant`, `IsServeurOrGerant`, and `IsCuisinierOrGerant`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-model-rbac/02-CONTEXT.md
@.planning/phases/02-user-model-rbac/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement DRF Permissions for RBAC</name>
  <files>backend/apps/users/permissions.py, backend/apps/users/tests/test_permissions.py</files>
  <behavior>
    - Test 1: `IsGerant` allows GERANT, blocks others.
    - Test 2: `IsServeurOrGerant` allows SERVEUR and GERANT, blocks CUISINIER and CLIENT.
    - Test 3: `IsCuisinierOrGerant` allows CUISINIER and GERANT, blocks SERVEUR and CLIENT.
  </behavior>
  <action>Write unit tests in `test_permissions.py` mocking DRF Requests and Views to test permission logic. Then implement the permission classes in `permissions.py` extending `permissions.BasePermission`. Enforce hierarchical roles per D-05 and D-06.</action>
  <verify>
    <automated>python backend/manage.py test apps.users.tests.test_permissions -v 2</automated>
  </verify>
  <done>Permission classes strictly validate user roles and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted API requests being evaluated for access rights. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | `permissions.py` | mitigate | Explicitly block access if `request.user` is not authenticated or lacks the exact required role/hierarchy. |
</threat_model>

<verification>
- `python backend/manage.py test apps.users.tests.test_permissions` passes.
</verification>

<success_criteria>
- Permission classes handle all permutations of roles accurately.
- `GERANT` correctly inherits access for both `SERVEUR` and `CUISINIER` protected routes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-model-rbac/02-02-SUMMARY.md`
</output>