---
phase: 02-user-model-rbac
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/users/models.py
  - backend/apps/users/apps.py
  - backend/apps/users/admin.py
  - backend/tastify_backend/settings/base.py
  - backend/apps/users/tests/test_models.py
autonomous: true
requirements:
  - REQ-02-1
  - REQ-02-2
must_haves:
  truths:
    - The custom Utilisateur model is registered as the default Django auth model.
    - Users can have one of 4 roles, defaulting to CLIENT.
  artifacts:
    - path: backend/apps/users/models.py
      provides: Utilisateur model
    - path: backend/tastify_backend/settings/base.py
      provides: AUTH_USER_MODEL configuration
  key_links:
    - from: backend/tastify_backend/settings/base.py
      to: backend/apps/users/models.py
      via: AUTH_USER_MODEL
---

<objective>
Establish the custom `Utilisateur` model and register it as the default Django authentication model.

Purpose: Django requires the custom user model to be set up before any migrations to avoid database foreign-key issues. This lays the foundation for all future identity and RBAC features.
Output: A test-covered Django app `users` containing the `Utilisateur` model, updated settings, and initial database migration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-model-rbac/02-CONTEXT.md
@.planning/phases/02-user-model-rbac/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold `users` app and Write Model Tests</name>
  <files>backend/apps/users/apps.py, backend/apps/users/__init__.py, backend/apps/users/tests/test_models.py</files>
  <action>Create the users app directory structure inside `backend/apps/`. Define `UsersConfig` in `apps.py` (name='apps.users'). Write unit tests in `test_models.py` verifying that `Utilisateur` extends `AbstractUser`, has the correct `Role` TextChoices, and defaults to `CLIENT` per D-01 and D-02.</action>
  <verify>
    <automated>python backend/manage.py test apps.users.tests.test_models || true</automated>
  </verify>
  <done>App structure created, test file committed and expected to fail initially.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Utilisateur Model and Configure Settings</name>
  <files>backend/apps/users/models.py, backend/tastify_backend/settings/base.py</files>
  <behavior>
    - Test 1: `Utilisateur.Role.GERANT` exists and evaluates to "GERANT".
    - Test 2: Creating a user assigns `CLIENT` role by default.
  </behavior>
  <action>Implement `Utilisateur` extending `AbstractUser`. Add `Role` as `models.TextChoices` (GERANT, SERVEUR, CUISINIER, CLIENT). Add `role` field. In `base.py`, add `apps.users` to `INSTALLED_APPS` and set `AUTH_USER_MODEL = 'users.Utilisateur'`.</action>
  <verify>
    <automated>python backend/manage.py test apps.users.tests.test_models -v 2</automated>
  </verify>
  <done>Tests pass, user model is the default for Django.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Admin and Create Initial Migrations</name>
  <files>backend/apps/users/admin.py, backend/apps/users/migrations/0001_initial.py</files>
  <action>Create `UtilisateurAdmin` extending `UserAdmin` to include the `role` field in `fieldsets` and `list_display` per D-01 and D-02. Run `python backend/manage.py makemigrations users` and `python backend/manage.py migrate` to apply the initial schema to the DB.</action>
  <verify>
    <automated>python backend/manage.py showmigrations users</automated>
  </verify>
  <done>Admin registered securely and database migrated successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Django Admin -> Database | Administrators managing user roles and passwords. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | `Utilisateur` model | mitigate | Limit `role` modification to Superusers via `UtilisateurAdmin`. Future API serializers must make `role` read-only for non-gerants. |
| T-02-02 | Information Disclosure | `AUTH_USER_MODEL` | accept | User data is secured by built-in Django session/auth mechanics (JWT later). No immediate API exposure. |
</threat_model>

<verification>
- `python backend/manage.py test apps.users.tests.test_models` passes.
- Database contains the `users_utilisateur` table (via migrations).
- `AUTH_USER_MODEL` is correctly pointing to `users.Utilisateur`.
</verification>

<success_criteria>
- The custom user model is fully integrated with Django's auth system.
- Roles are strictly typed using `TextChoices`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-model-rbac/02-01-SUMMARY.md`
</output>