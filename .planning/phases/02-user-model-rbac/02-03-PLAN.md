---
phase: 02-user-model-rbac
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - backend/apps/users/management/commands/seed_dev.py
  - backend/apps/users/tests/test_commands.py
autonomous: true
requirements:
  - REQ-02-5
must_haves:
  truths:
    - Development environment can be seeded with one test user per role.
  artifacts:
    - path: backend/apps/users/management/commands/seed_dev.py
      provides: Django management command
  key_links:
    - from: backend/apps/users/management/commands/seed_dev.py
      to: backend/apps/users/models.py
      via: Utilisateur.objects.create_user
---

<objective>
Create a database seeding utility for local development.

Purpose: Accelerate developer experience by providing a single command to generate a complete set of test users (one for each role).
Output: Django management command `seed_dev` and its associated tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-user-model-rbac/02-CONTEXT.md
@.planning/phases/02-user-model-rbac/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement seed_dev Management Command</name>
  <files>backend/apps/users/management/commands/seed_dev.py, backend/apps/users/tests/test_commands.py</files>
  <behavior>
    - Test 1: Running the command creates exactly 4 distinct users.
    - Test 2: Users are created with the correct roles and securely hashed passwords.
  </behavior>
  <action>Create the directories `backend/apps/users/management/commands/` with `__init__.py` files. Write unit tests for the command. Implement `seed_dev.py` extending `BaseCommand`. Use `Utilisateur.objects.create_user()` to ensure passwords are hashed. Create one GERANT, SERVEUR, CUISINIER, and CLIENT test account per D-07.</action>
  <verify>
    <automated>python backend/manage.py test apps.users.tests.test_commands -v 2</automated>
  </verify>
  <done>Command runs without error, tests pass, and database correctly seeded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer Environment | Local database populated with dummy data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | `seed_dev.py` | mitigate | Ensure command only generates dummy credentials (e.g., `password123`) and does not rely on production secrets. |
</threat_model>

<verification>
- `python backend/manage.py test apps.users.tests.test_commands` passes.
- `python backend/manage.py seed_dev` executes without error.
</verification>

<success_criteria>
- Fast and reliable developer setup achieved.
- All 4 roles are represented in the seeded data.
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-model-rbac/02-03-SUMMARY.md`
</output>