# Plan 19-01: Foundation & Navigation

## 1. Goal
Set up the core types, update navigation, and create the basic page shell for the Stock module.

## 2. Tasks
- [ ] Create `app/frontend/backoffice/src/pages/Stock/types.ts` with `Ingredient` and `PlatIngredient` interfaces.
- [ ] Update `app/frontend/backoffice/src/components/layout/Sidebar.tsx` to point to `/stock` and update `allowedRoles`.
- [ ] Update `app/frontend/backoffice/src/App.tsx` to register the `/stock` route.
- [ ] Create `app/frontend/backoffice/src/pages/Stock/index.tsx` as a basic page shell with a "Stock" title.

## 3. Verification
- [ ] Verify that "Stock" appears in the sidebar for `GERANT` and `CUISINIER`.
- [ ] Verify that clicking "Stock" navigates to the new page.
- [ ] Verify that `tsc -b` passes in the backoffice frontend.
