---
phase: 06-plats-model-api
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [backend/apps/menu/management/commands/seed_menu.py, backend/apps/menu/tests/test_plats_api.py]
autonomous: true
requirements: [PLAT-05]
must_haves:
  truths:
    - "python backend/manage.py seed_menu successfully populates Categories and Plats"
    - "Integration tests for Plat API pass successfully"
  artifacts:
    - path: "backend/apps/menu/management/commands/seed_menu.py"
      provides: "Menu seeding command"
    - path: "backend/apps/menu/tests/test_plats_api.py"
      provides: "Integration tests for Plat API"
---

<objective>
Provide dev seeding for categories and dishes and implement comprehensive integration tests for the Plat API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-plats-model-api/06-CONTEXT.md
@backend/apps/menu/models.py
@backend/apps/menu/tests/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed_menu command</name>
  <files>backend/apps/menu/management/commands/seed_menu.py</files>
  <action>
    Create a Django management command to seed categories and dishes.
    - Path: backend/apps/menu/management/commands/seed_menu.py.
    - Ensure it handles existing data (don't create duplicates).
    - Seed at least 3 categories (e.g., Entrées, Plats Principaux, Desserts).
    - Seed at least 3 dishes per category with realistic prices and prep times.
    - Use the style.SUCCESS and stdout.write to provide feedback.
  </action>
  <verify>
    <automated>python backend/manage.py seed_menu</automated>
  </verify>
  <done>seed_menu command created and functional.</done>
</task>

<task type="auto">
  <name>Task 2: Implement integration tests for Plat API</name>
  <files>backend/apps/menu/tests/test_plats_api.py</files>
  <action>
    Create integration tests for the Plat API following the pattern in backend/apps/menu/tests/.
    
    Test Cases:
    - test_list_plats: Authenticated users can list dishes.
    - test_create_plat_as_gerant: Gerant can create a dish.
    - test_create_plat_as_serveur_forbidden: Serveur cannot create a dish.
    - test_soft_delete_plat: Gerant can soft-delete a dish.
    - test_visibility_filtering: Non-gerant only sees active and available dishes.
    - test_plat_detail_access: Proper 404 for inactive dishes for non-gerants.
  </action>
  <verify>
    <automated>python backend/manage.py test apps.menu.tests.test_plats_api</automated>
  </verify>
  <done>Plat API integration tests implemented and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI -> DB | Seeding script access to DB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Seeding Script | mitigate | Ensure seeding script is only used in development/controlled environments |
</threat_model>

<success_criteria>
- Seeding script works and populates the database with test menu data.
- Integration tests cover CRUD, RBAC, and visibility logic for dishes.
</success_criteria>

<output>
After completion, create .planning/phases/06-plats-model-api/06-03-SUMMARY.md
</output>
