---
phase: 06-plats-model-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/apps/menu/models.py, backend/apps/menu/admin.py]
autonomous: true
requirements: [PLAT-01, PLAT-03]
must_haves:
  truths:
    - "Plat model exists in backend/apps/menu/models.py"
    - "Plat model has a ForeignKey to Categorie"
    - "Soft-delete pattern (est_active) is implemented on Plat"
    - "Migrations are generated and applied successfully"
  artifacts:
    - path: "backend/apps/menu/models.py"
      provides: "Plat model definition"
    - path: "backend/apps/menu/admin.py"
      provides: "Django admin registration for Plat"
---

<objective>
Implement the Plat (Dish) model in the menu app with support for soft-delete and association with Categorie.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-plats-model-api/06-CONTEXT.md
@backend/apps/menu/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Plat model</name>
  <files>backend/apps/menu/models.py</files>
  <action>
    Add the Plat model to backend/apps/menu/models.py following the pattern established for Categorie.
    
    Fields (per D-06-02, D-06-03):
    - categorie: ForeignKey to Categorie, on_delete=models.CASCADE, related_name='plats'.
    - nom: CharField(max_length=100).
    - description: TextField(blank=True).
    - prix: DecimalField(max_digits=10, decimal_places=2).
    - temps_preparation: IntegerField(default=15) - duration in minutes.
    - image: ImageField(upload_to='plats/', blank=True, null=True) - per D-06-04.
    - est_disponible: BooleanField(default=True) - visibility for ordering.
    - est_active: BooleanField(default=True) - soft-delete flag.
    - created_at: DateTimeField(auto_now_add=True).
    - updated_at: DateTimeField(auto_now=True).

    Implementation Details:
    - Reuse or create a PlatQuerySet and PlatManager similar to Categorie.
    - Override delete() for soft-delete (sets est_active=False).
    - Define __str__ returning the name.
    - Meta: ordering by ['categorie', 'nom'], verbose_name='Plat'.
  </action>
  <verify>
    <automated>python backend/manage.py check</automated>
  </verify>
  <done>Plat model defined with all fields and soft-delete logic.</done>
</task>

<task type="auto">
  <name>Task 2: Register Plat in admin</name>
  <files>backend/apps/menu/admin.py</files>
  <action>
    Register the Plat model in backend/apps/menu/admin.py.
    Include list_display for nom, categorie, prix, temps_preparation, est_disponible, and est_active.
    Add list_filter for categorie, est_disponible, and est_active.
    Add search_fields for nom and description.
  </action>
  <verify>
    <automated>python backend/manage.py check</automated>
  </verify>
  <done>Plat model registered in Django admin.</done>
</task>

<task type="auto">
  <name>Task 3: Generate and apply migrations</name>
  <files>backend/apps/menu/migrations/*.py</files>
  <action>
    Generate the migration for the Plat model and apply it to the database.
  </action>
  <verify>
    <automated>python backend/manage.py makemigrations menu; python backend/manage.py migrate menu</automated>
  </verify>
  <done>Database schema updated with Plat table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB -> Model | Model constraints (max_digits, decimal_places) protect data integrity |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Plat Model | mitigate | Use DecimalField for price to avoid floating point errors (D-06-03) |
</threat_model>

<success_criteria>
- Plat model exists and is correctly linked to Categorie.
- Soft-delete logic is functional at the model level.
- Migrations are applied.
</success_criteria>

<output>
After completion, create .planning/phases/06-plats-model-api/06-01-SUMMARY.md
</output>
