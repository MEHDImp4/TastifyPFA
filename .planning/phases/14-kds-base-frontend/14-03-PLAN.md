---
phase: 14-kds-base-frontend
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified: [
  frontend/back-office/src/pages/Kds/KdsPage.tsx,
  frontend/back-office/src/pages/Kds/components/TicketCard.tsx,
  frontend/back-office/src/pages/Kds/components/KdsTimer.tsx
]
autonomous: true
requirements: [KDS-BASE-03, KDS-BASE-04]

must_haves:
  truths:
    - "KDS UI uses horizontal scrolling for tickets"
    - "Tickets display table ID, items, quantities, and prep notes"
    - "Tickets show an elapsed time timer that color-codes by duration"
    - "UI follows the ECO-FRESH palette from DESIGN.md"
  artifacts:
    - path: "frontend/back-office/src/pages/Kds/KdsPage.tsx"
      provides: "Main horizontal-rail layout for KDS"
    - path: "frontend/back-office/src/pages/Kds/components/TicketCard.tsx"
      provides: "Vertical ticket component"
    - path: "frontend/back-office/src/pages/Kds/components/KdsTimer.tsx"
      provides: "Performance-optimized timer"
---

<objective>
Build the KDS User Interface. This includes the main horizontal scroll layout, the vertical ticket cards, and the specialized timer component that provides visual cues for preparation delays.

Purpose: Provide the visual interface for kitchen staff to manage orders.
Output: KdsPage and associated components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DESIGN.md
@.planning/phases/14-kds-base-frontend/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build KDS Layout (Horizontal Rail)</name>
  <files>
    - frontend/back-office/src/pages/Kds/KdsPage.tsx
    - frontend/back-office/src/pages/Kds/KdsPage.test.tsx
  </files>
  <action>
    - Create the main `KdsPage` component.
    - Create `frontend/back-office/src/pages/Kds/KdsPage.test.tsx` for smoke testing.
    - Use Tailwind classes for horizontal scrolling: `flex overflow-x-auto h-full gap-4 snap-x`.
    - Implement the `onWheel` listener to translate vertical scroll to horizontal scroll for desktop users.
    - Apply the ECO-FRESH background (`#1a323b`).
    - Integrate `useKdsStore` to map over active orders.
    - Add the `/kds` route to the main router and protect it with `CUISINIER` or `GERANT` roles.
  </action>
  <verify>
    <automated>npm test frontend/back-office/src/pages/Kds/KdsPage.test.tsx</automated>
  </verify>
  <done>KDS layout structure completed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TicketCard component</name>
  <files>
    - frontend/back-office/src/pages/Kds/components/TicketCard.tsx
    - frontend/back-office/src/pages/Kds/components/TicketCard.test.tsx
  </files>
  <action>
    - Create the `TicketCard` component.
    - Create the unit test `frontend/back-office/src/pages/Kds/components/TicketCard.test.tsx`.
    - UI: Width `w-[320px]`, Surface Ardoise (`#264653`), Table ID, Items, Qty, Notes.
    - Ensure clean typography per DESIGN.md.
  </action>
  <verify>
    <automated>npm test frontend/back-office/src/pages/Kds/components/TicketCard.test.tsx</automated>
  </verify>
  <done>Ticket UI component completed.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Performance-Optimized KdsTimer</name>
  <files>
    - frontend/back-office/src/pages/Kds/components/KdsTimer.tsx
    - frontend/back-office/src/pages/Kds/components/KdsTimer.test.tsx
  </files>
  <action>
    - Create the `KdsTimer` component.
    - Create the unit test `frontend/back-office/src/pages/Kds/components/KdsTimer.test.tsx`.
    - Implement the centralized "now" state or interval logic.
    - Logic: < 10m (Green), 10-20m (Orange), > 20m (Red + Pulse).
  </action>
  <verify>
    <automated>npm test frontend/back-office/src/pages/Kds/components/KdsTimer.test.tsx</automated>
  </verify>
  <done>Optimized timer component completed.</done>
</task>

</tasks>

<success_criteria>
- KDS UI is responsive and scrolls horizontally.
- Tickets are legible and follow the design system.
- Timers accurately reflect preparation time.
</success_criteria>

<output>
After completion, create `.planning/phases/14-kds-base-frontend/14-03-SUMMARY.md`
</output>
