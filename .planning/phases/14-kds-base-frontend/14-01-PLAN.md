---
phase: 14-kds-base-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/apps/commandes/views.py]
autonomous: true
requirements: [KDS-BASE-01]

must_haves:
  truths:
    - "CUISINIER role can access the orders API"
    - "CUISINIER sees all orders with statut=EN_CUISINE regardless of ownership"
    - "SERVEUR still only sees their own orders (unless filtering by table)"
  artifacts:
    - path: "backend/apps/commandes/views.py"
      provides: "Updated filtering logic and permissions for KDS"
  key_links:
    - from: "CommandeViewSet"
      to: "IsCuisinierOrGerant"
      via: "permission_classes"
---

<objective>
Update the backend Commandes API to support the KDS requirements. This involves allowing the CUISINIER role to access orders and ensuring they can see all active orders in the kitchen, not just their own.

Purpose: Enable the KDS frontend to fetch its initial data.
Output: Modified CommandeViewSet with updated permissions and queryset filtering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-kds-base-frontend/14-RESEARCH.md
@backend/apps/commandes/views.py
@backend/apps/users/permissions.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CommandeViewSet Permissions</name>
  <files>
    - backend/apps/commandes/views.py
    - backend/apps/commandes/tests/test_kds_permissions.py
  </files>
  <action>
    - Create `backend/apps/commandes/tests/test_kds_permissions.py` with cases for CUISINIER permission.
    - Modify `CommandeViewSet.permission_classes` in `backend/apps/commandes/views.py` to allow `CUISINIER` role.
    - Use `(IsServeurOrGerant | IsCuisinierOrGerant)` to allow both roles access to the ViewSet.
    - Ensure `IsCuisinierOrGerant` is imported from `apps.users.permissions`.
  </action>
  <verify>
    <automated>pytest backend/apps/commandes/tests/test_kds_permissions.py</automated>
  </verify>
  <done>Permission classes updated and verified by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update get_queryset for CUISINIER role</name>
  <files>
    - backend/apps/commandes/views.py
    - backend/apps/commandes/tests/test_kds_permissions.py
  </files>
  <behavior>
    - If user is CUISINIER and filtering by `statut=EN_CUISINE`, they should see all orders matching that status, even if they aren't the `serveur`.
    - If user is CUISINIER and NOT filtering by status, they should probably see all active orders (or we can restrict to EN_CUISINE by default for them). Per research: `qs = qs.filter(statut=Commande.Statut.EN_CUISINE)`.
    - SERVEUR filtering logic must remain unchanged (ownership-based).
  </behavior>
  <action>
    - Add tests to `backend/apps/commandes/tests/test_kds_permissions.py` for queryset filtering.
    - Update `get_queryset` in `CommandeViewSet`:
    ```python
    if user.role == 'CUISINIER':
        # Cuisinier sees all orders currently in the kitchen
        qs = qs.filter(statut=Commande.Statut.EN_CUISINE)
    elif user.role != 'GERANT' and not table_id:
        # SERVEUR still restricted to own orders
        qs = qs.filter(serveur=user)
    ```
  </action>
  <verify>
    <automated>pytest backend/apps/commandes/tests/test_kds_permissions.py</automated>
  </verify>
  <done>Queryset filtering logic correctly handles CUISINIER role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | CUISINIER role accessing data previously restricted to SERVEUR/GERANT. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | CommandeViewSet | mitigate | Strict role-based filtering in `get_queryset` ensures CUISINIER only sees kitchen-relevant orders. |
</threat_model>

<success_criteria>
- CUISINIER can fetch orders from `/api/commandes/?statut=EN_CUISINE`.
- CUISINIER sees orders created by other servers.
- SERVEUR cannot see orders created by others (regression test).
</success_criteria>

<output>
After completion, create `.planning/phases/14-kds-base-frontend/14-01-SUMMARY.md`
</output>
