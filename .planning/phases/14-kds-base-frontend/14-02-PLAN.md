---
phase: 14-kds-base-frontend
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [frontend/back-office/src/pages/Kds/store/useKdsStore.ts]
autonomous: true
requirements: [KDS-BASE-02]

must_haves:
  truths:
    - "KDS store fetches initial orders on mount"
    - "KDS store updates in real-time on WebSocket events"
    - "Orders are sorted newest first (LIFO)"
  artifacts:
    - path: "frontend/back-office/src/pages/Kds/store/useKdsStore.ts"
      provides: "Zustand store for kitchen orders"
---

<objective>
Implement the KDS state management layer using Zustand. This store will handle the initial REST API fetch and listen for real-time WebSocket events to keep the kitchen display in sync.

Purpose: Provide a single source of truth for kitchen orders that reacts to real-time events.
Output: useKdsStore hook/store.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-kds-base-frontend/14-RESEARCH.md
@frontend/_shared/websocket/useStaffSocketStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useKdsStore</name>
  <files>
    - frontend/back-office/src/pages/Kds/store/useKdsStore.ts
    - frontend/back-office/src/pages/Kds/store/useKdsStore.test.ts
  </files>
  <behavior>
    - `orders` state initialized as empty array.
    - `fetchOrders()`: calls API, populates `orders`.
    - `addOrUpdateOrder(order)`: if order exists, update it; if not, `unshift` (LIFO).
    - `removeOrder(orderId)`: remove from list.
    - `handleSocketEvent(event)`: filters for `order_created`/`order_updated` and `statut="EN_CUISINE"`.
  </behavior>
  <action>
    - Create the Zustand store in `frontend/back-office/src/pages/Kds/store/useKdsStore.ts`.
    - Create the unit test `frontend/back-office/src/pages/Kds/store/useKdsStore.test.ts`.
    - Implement the logic to handle both initial load and incoming socket events.
    - Ensure LIFO sorting by using `unshift()` or sorting by `created_at` DESC.
  </action>
  <verify>
    <automated>npm test frontend/back-office/src/pages/Kds/store/useKdsStore.test.ts</automated>
  </verify>
  <done>Zustand store implemented with unit tests for logic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire KDS Store to WebSocket events</name>
  <files>
    - frontend/back-office/src/pages/Kds/KdsSocketManager.tsx
    - frontend/back-office/src/pages/Kds/KdsSocketManager.test.tsx
  </files>
  <action>
    - Create `frontend/back-office/src/pages/Kds/KdsSocketManager.tsx`.
    - Create `frontend/back-office/src/pages/Kds/KdsSocketManager.test.tsx`.
    - Implement the subscription logic:
    ```typescript
    const lastEvent = useStaffSocketStore(s => s.lastEvent);
    const handleEvent = useKdsStore(s => s.handleSocketEvent);
    useEffect(() => {
      if (lastEvent) handleEvent(lastEvent);
    }, [lastEvent]);
    ```
  </action>
  <verify>
    <automated>npm test frontend/back-office/src/pages/Kds/KdsSocketManager.test.tsx</automated>
  </verify>
  <done>WebSocket events are flowing into the KDS store.</done>
</task>

</tasks>

<success_criteria>
- `useKdsStore` correctly manages the list of orders.
- New orders are added to the front of the list.
- Orders leaving the `EN_CUISINE` state are removed from the list.
</success_criteria>

<output>
After completion, create `.planning/phases/14-kds-base-frontend/14-02-SUMMARY.md`
</output>
