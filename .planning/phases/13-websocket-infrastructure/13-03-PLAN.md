---
phase: 13-websocket-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified: [
  "README.md",
  "docs/brain/00_Meta/FILE_MAP.md",
  "docs/brain/02_Journal/CHANGELOG.md",
  "dashboard.html",
  ".planning/STATE.md",
  ".planning/phases/13-websocket-infrastructure/13-03-SUMMARY.md"
]
autonomous: true
requirements: [WS-01, WS-02, WS-03, WS-04, WS-05, WS-06]

must_haves:
  truths:
    - "Backend focused WebSocket tests pass"
    - "Frontend provider tests pass"
    - "Docker services can run the backend against Redis for a real channel-layer smoke"
    - "A logged-in staff browser session receives the shell-triggered infrastructure event"
    - "Idle socket behavior is observed without unexpected disconnect/reconnect during the heartbeat verification window"
    - "Project dashboard and Obsidian brain reflect Phase 13 implementation state"
  artifacts:
    - path: "docs/brain/00_Meta/FILE_MAP.md"
      provides: "Updated map for new WebSocket source and test files"
    - path: "README.md"
      provides: "High-level project structure and realtime infrastructure notes"
    - path: "docs/brain/02_Journal/CHANGELOG.md"
      provides: "Timestamped Phase 13 execution entries with commit hashes"
    - path: "dashboard.html"
      provides: "Accurate current project state"
    - path: ".planning/STATE.md"
      provides: "Planning state updated after Phase 13 execution"
  key_links:
    - from: "backend/core/realtime.py"
      to: "Redis channel layer"
      via: "Docker backend smoke using `broadcast_staff_event`"
      pattern: "broadcast_staff_event"
    - from: "frontend/_shared/websocket/WebSocketProvider.tsx"
      to: "backend/core/routing.py"
      via: "/ws/staff/?token=<jwt>"
      pattern: "ws/staff"
---

<objective>
Run end-to-end hardening and synchronize project state for Phase 13.

Purpose: Prove the backend and frontend slices work together at the infrastructure level, then keep dashboard and brain documentation consistent with the new WebSocket files and commits.
Output: Passing focused tests/builds, Docker Redis smoke evidence, updated dashboard/brain/state artifacts, and final Phase 13 summary.
</objective>

<execution_context>
@C:/Users/mehdi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mehdi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-websocket-infrastructure/13-CONTEXT.md
@.planning/phases/13-websocket-infrastructure/13-RESEARCH.md
@.planning/phases/13-websocket-infrastructure/13-01-SUMMARY.md
@.planning/phases/13-websocket-infrastructure/13-02-SUMMARY.md
@docker-compose.yml
@docs/brain/00_Meta/FILE_MAP.md
@docs/brain/02_Journal/CHANGELOG.md
@dashboard.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run focused backend and frontend verification gates</name>
  <files>backend/core/tests/test_websocket_auth.py, backend/core/tests/test_staff_consumer.py, frontend/back-office/src/websocket/WebSocketProvider.test.tsx</files>
  <action>
    Run the focused Phase 13 test commands. If either command fails, stop and fix the root cause in the owning files from Plan 13-01 or Plan 13-02 before continuing. Do not broaden scope into KDS/order domain event logic.
  </action>
  <verify>
    <automated>docker compose exec backend pytest core/tests/test_websocket_auth.py core/tests/test_staff_consumer.py -q; cd frontend/back-office; npm run test -- src/websocket/WebSocketProvider.test.tsx --run</automated>
  </verify>
  <done>Focused backend communicator tests and frontend provider tests are green in one verification pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run Docker Redis channel-layer smoke and full build gates</name>
  <files>docker-compose.yml, backend/core/realtime.py, backend/core/consumers.py, frontend/_shared/websocket/WebSocketProvider.tsx</files>
  <action>
    Start or reuse Docker Compose services and run one smoke that exercises the Redis-backed channel layer from the backend container. Use the existing `redis` service and `CHANNEL_LAYERS`; do not replace it with the in-memory layer. Then run full backend pytest and back-office build. The smoke must use a Django shell snippet that calls `broadcast_staff_event("infrastructure_test", {"source": "phase_13"})` after services are healthy; record the exact command and outcome in the summary. With a logged-in staff browser session open, confirm the event is visible through the shared websocket Zustand store and/or browser console, then keep the session idle through the observation window recorded in the summary to cover the native heartbeat/ping-pong expectation from the locked context.
  </action>
  <verify>
    <automated>docker compose up -d; docker compose exec backend python manage.py shell -c "from core.realtime import broadcast_staff_event; broadcast_staff_event('infrastructure_test', {'source': 'phase_13'}); print('PHASE_13_BROADCAST_SENT')"; docker compose exec backend pytest -q; cd frontend/back-office; npm run build</automated>
  </verify>
  <done>Docker backend tests pass against the composed Redis service, the infrastructure event is observed in the live staff session, and the idle heartbeat observation is recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize dashboard, brain, state, and Phase 13 summary</name>
  <files>README.md, docs/brain/00_Meta/FILE_MAP.md, docs/brain/02_Journal/CHANGELOG.md, dashboard.html, .planning/STATE.md, .planning/phases/13-websocket-infrastructure/13-03-SUMMARY.md</files>
  <action>
    Update `FILE_MAP.md` with every new backend and frontend WebSocket source/test file from Plans 13-01 and 13-02. Update `README.md` if the new shared WebSocket structure or realtime infrastructure notes change the high-level project map. Append timestamped `CHANGELOG.md` entries for Phase 13 implementation commits with commit hashes after commits exist. Update `dashboard.html` and `.planning/STATE.md` so they accurately show Phase 13 WebSocket Infrastructure as implemented/verified or identify any remaining verification gap by exact command. Create the final `13-03-SUMMARY.md` with test results, Docker smoke outcome, changed files, and commit hashes. Preserve the no-push rule.
  </action>
  <verify>
    <automated>git status --short; Select-String -Path docs/brain/00_Meta/FILE_MAP.md -Pattern "WebSocketProvider|StaffConsumer|JWTAuthMiddleware"; Select-String -Path docs/brain/02_Journal/CHANGELOG.md -Pattern "Phase 13|WebSocket"; Select-String -Path dashboard.html -Pattern "Phase 13|WebSocket"</automated>
  </verify>
  <done>Project tracking files reflect Phase 13 accurately, and the summary records verification evidence and commit hashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner -> Docker services | Verification depends on composed MySQL, Redis, backend, and frontend build environments. |
| Implementation state -> dashboard/brain | Generated project status is read by future GSD phases and must not overstate verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | R | Phase 13 verification records | mitigate | Record exact focused tests, Docker smoke, and build outcomes in `13-03-SUMMARY.md` and changelog entries. |
| T-13-11 | I | Smoke command output | mitigate | Do not print JWT values or full WebSocket URLs in logs, dashboard, changelog, or summary. |
| T-13-12 | T | Project state files | mitigate | Update `dashboard.html`, `STATE.md`, `FILE_MAP.md`, and `CHANGELOG.md` from observed command outcomes, not assumptions. |
</threat_model>

<verification>
- `docker compose exec backend pytest core/tests/test_websocket_auth.py core/tests/test_staff_consumer.py -q`
- `cd frontend/back-office; npm run test -- src/websocket/WebSocketProvider.test.tsx --run`
- `docker compose up -d; docker compose exec backend python manage.py shell -c "from core.realtime import broadcast_staff_event; broadcast_staff_event('infrastructure_test', {'source': 'phase_13'}); print('PHASE_13_BROADCAST_SENT')"`
- `docker compose exec backend pytest -q`
- `cd frontend/back-office; npm run build`
- Manual: keep a logged-in staff browser session open, trigger the infrastructure broadcast, and confirm the event appears in the shared websocket Zustand store and/or browser console.
- Manual: leave the staff socket idle during the observation window documented in the summary and confirm there is no unexpected disconnect/reconnect.
- `Select-String -Path README.md -Pattern "WebSocket|realtime|staff socket"`
- `Select-String -Path docs/brain/00_Meta/FILE_MAP.md -Pattern "WebSocketProvider|StaffConsumer|JWTAuthMiddleware"`
- `Select-String -Path dashboard.html -Pattern "Phase 13|WebSocket"`
</verification>

<success_criteria>
- Backend WebSocket communicator tests are green.
- Frontend WebSocket provider tests are green.
- Docker Compose verifies backend tests against Redis-backed Channels configuration.
- A Django-shell infrastructure broadcast is observed in the live staff session through the shared websocket store/console path.
- Idle socket behavior is observed and recorded as the Phase 13 heartbeat/ping-pong verification step.
- Back-office frontend build succeeds.
- Dashboard, Obsidian brain file map, changelog, state, and Phase 13 summary accurately reflect the completed infrastructure work.
</success_criteria>

<output>
After completion, create `.planning/phases/13-websocket-infrastructure/13-03-SUMMARY.md`
</output>
