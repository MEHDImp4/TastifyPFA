---
phase: 10-commandes-model
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: [
    "backend/apps/commandes/signals.py",
    "backend/apps/commandes/apps.py",
    "backend/apps/commandes/tests/test_signals.py"
]
autonomous: true
requirements: [COMM-05]

must_haves:
  truths:
    - "Adding a line to an order updates the order's total amount."
    - "Deleting a line from an order updates the order's total amount."
    - "Cancelled lines are excluded from the total amount."
  artifacts:
    - path: "backend/apps/commandes/signals.py"
      provides: "Order total calculation logic"
  key_links:
    - from: "CommandeLigne.save/delete"
      to: "recalcul_montant_total"
      via: "Django signals"
---

<objective>
Implement and verify Django signals for automatic calculation of the Commande montant_total.

Purpose: Ensure the order total is always synchronized with its active lines.
Output: Functioning signals and a comprehensive test suite for financial accuracy.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-commandes-model/10-RESEARCH.md
@.planning/phases/10-commandes-model/10-PATTERNS.md
@.planning/phases/10-commandes-model/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement signals and wire in apps.py</name>
  <files>
    backend/apps/commandes/signals.py,
    backend/apps/commandes/apps.py
  </files>
  <action>
    - Create `backend/apps/commandes/signals.py`.
    - Implement `recalcul_montant_total` function using `post_save` and `post_delete` receivers on `CommandeLigne` per D-09.
    - Use `aggregate(Sum(F('quantite') * F('prix_unitaire')))` and exclude lines with status `ANNULE`.
    - Ensure `update_fields=['montant_total', 'updated_at']` is used on `commande.save()`.
    - Update `backend/apps/commandes/apps.py` to import the signals module in the `ready()` method.
  </action>
  <verify>
    <automated>python backend/manage.py check</automated>
  </verify>
  <done>Signals implemented and wired correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive signal verification</name>
  <files>
    backend/apps/commandes/tests/test_signals.py
  </files>
  <action>
    - Create `backend/apps/commandes/tests/test_signals.py`.
    - Write tests for:
        - Total updates when a line is added.
        - Total updates when a line quantity is changed.
        - Total updates when a line is deleted.
        - Total updates when a line status is changed to `ANNULE` (should be excluded).
        - Total updates when a line status is changed FROM `ANNULE` to `EN_ATTENTE`.
        - Total is 0 when no lines exist.
    - Ensure tests do not use bulk operations (which bypass signals).
  </action>
  <verify>
    <automated>python backend/manage.py test apps.commandes.tests.test_signals</automated>
  </verify>
  <done>Signals verified with all edge cases passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Signal -> Model | Updating parent record based on child changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | Commande.montant_total | mitigate | Force recalculation from source of truth (lines) via backend signals, ignoring cancelled lines. |
</threat_model>

<verification>
Run the entire commandes test suite: `python backend/manage.py test apps.commandes`.
</verification>

<success_criteria>
1. Signals correctly recalculate total on all relevant CommandeLigne changes.
2. Cancelled lines are excluded from total.
3. Total resets to 0 when empty.
4. All signal tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-commandes-model/10-03-SUMMARY.md`
</output>
