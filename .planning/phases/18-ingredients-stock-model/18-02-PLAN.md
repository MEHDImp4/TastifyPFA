---
phase: "18-ingredients-stock-model"
plan: "18-02"
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - "app/backend/apps/stock/serializers.py"
  - "app/backend/apps/stock/views.py"
  - "app/backend/apps/stock/urls.py"
  - "app/backend/tastify_backend/urls.py"
  - "app/backend/apps/stock/tests/test_api.py"
autonomous: true
requirements: ["REQ-01"]
must_haves:
  truths:
    - "`GERANT` can create, update, and soft-delete ingredients."
    - "API exposes base units natively without backend conversion."
    - "Active filtering prevents soft-deleted items from appearing in normal lists."
  artifacts:
    - path: "app/backend/apps/stock/views.py"
      provides: "IngredientViewSet for REST operations"
    - path: "app/backend/apps/stock/serializers.py"
      provides: "IngredientSerializer and PlatIngredientSerializer"
  key_links:
    - from: "app/backend/tastify_backend/urls.py"
      to: "app/backend/apps/stock/urls.py"
      via: "include('apps.stock.urls')"
---

<objective>
Implement DRF serializers, viewsets, and routing for managing Ingredients and their links to Plats (PlatIngredients). Enforce `IsGerant` permission for write operations.

Purpose: Provide REST API access for the ingredients functionality.
Output: API endpoints for Ingredients mapped correctly.
</objective>

<context>
@.planning/phases/18-ingredients-stock-model/18-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: API Serializers and Views</name>
  <files>app/backend/apps/stock/serializers.py, app/backend/apps/stock/views.py, app/backend/apps/stock/urls.py, app/backend/tastify_backend/urls.py</files>
  <action>
    1. Create `apps/stock/serializers.py`. Define `IngredientSerializer` and `PlatIngredientSerializer`.
    2. Create `apps/stock/views.py`. Define `IngredientViewSet` extending `ModelViewSet`. Ensure `get_queryset` filters out `est_active=False` for standard listing.
    3. Apply standard authentication and authorization. Set `permission_classes = [IsAuthenticated, IsGerant]` for the `IngredientViewSet` to ensure only managers modify stock base configs.
    4. Create `apps/stock/urls.py` and register the `IngredientViewSet` to a router. Link it into the project's main URL configuration (`urls.py`).
  </action>
  <verify>
    <automated>docker compose exec backend python manage.py check</automated>
  </verify>
  <done>Views and URLs are configured and project check succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: API Tests</name>
  <files>app/backend/apps/stock/tests/test_api.py</files>
  <action>
    Write tests in `apps/stock/tests/test_api.py` validating the CRUD operations and enforcing that only GERANT users have write access. Also test that standard listing filters out soft-deleted ingredients.
  </action>
  <verify>
    <automated>docker compose exec backend pytest apps/stock/tests/test_api.py -x</automated>
  </verify>
  <done>All tests pass confirming CRUD, permissions, and active filtering.</done>
</task>
</tasks>
