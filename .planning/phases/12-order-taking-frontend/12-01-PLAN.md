---
phase: 12-order-taking-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/salle/package.json",
  "frontend/salle/src/App.tsx",
  "frontend/salle/src/main.tsx",
  "frontend/salle/src/pages/Ordering/store/useOrderStore.ts",
  "frontend/salle/src/pages/Ordering/store/useOrderStore.test.ts",
  "frontend/salle/src/pages/Ordering/OrderingPage.tsx",
  "frontend/salle/src/pages/Map/MapView.tsx"
]
autonomous: true
requirements: [ORD-FE-01]

must_haves:
  truths:
    - "Navigating to /tables/:id/order renders the OrderingPage"
    - "useOrderStore manages separate carts per tableId"
    - "Clicking a table in MapView redirects to its ordering page"
  artifacts:
    - path: "frontend/salle/src/pages/Ordering/store/useOrderStore.ts"
      provides: "Zustand Map-Registry store"
    - path: "frontend/salle/src/pages/Ordering/OrderingPage.tsx"
      provides: "Main ordering controller"
  key_links:
    - from: "MapView.tsx"
      to: "OrderingPage.tsx"
      via: "react-router-dom Link/useNavigate"
---

<objective>
Setup the foundational infrastructure for the ordering flow, including routing and multi-table cart state management.

Purpose: Provide the routing skeleton and isolated state needed for the menu browser.
Output: react-router-dom installed, useOrderStore implemented, and basic page routing working.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/salle/src/App.tsx
@frontend/salle/src/pages/Map/MapView.tsx
@.planning/phases/12-order-taking-frontend/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router-dom and configure routing</name>
  <files>frontend/salle/package.json, frontend/salle/src/main.tsx, frontend/salle/src/App.tsx</files>
  <action>
    - Install `react-router-dom` in `frontend/salle`.
    - Wrap the app in `BrowserRouter` in `main.tsx`.
    - Refactor `App.tsx` to use `Routes` and `Route`:
      - `/` -> `MapView`
      - `/tables/:id/order` -> `OrderingPage` (skeleton for now).
    - Ensure `handleLogout` still works and auth check is respected.
  </action>
  <verify>
    <automated>cd frontend/salle; npm install react-router-dom; npm run build</automated>
  </verify>
  <done>Routing is active and /tables/1/order doesn't 404 in the app</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useOrderStore with Map-Registry pattern</name>
  <files>frontend/salle/src/pages/Ordering/store/useOrderStore.ts, frontend/salle/src/pages/Ordering/store/useOrderStore.test.ts</files>
  <behavior>
    - `addItem(tableId, plat)`: Adds 1 to quantity if exists, else adds new item.
    - `removeItem(tableId, platId)`: Decrements quantity, removes if 0.
    - `clearCart(tableId)`: Resets cart for specific table.
    - `getCart(tableId)`: Returns items for that table or [].
    - `getCartTotal(tableId)`: Calculates total based on items and prices.
  </behavior>
  <action>
    Implement Zustand store using a `Record<number, CartItem[]>` to isolate carts per table.
    Follow the "Map-Registry" pattern from RESEARCH.md.
  </action>
  <verify>
    <automated>cd frontend/salle; npm test -- useOrderStore.test.ts</automated>
  </verify>
  <done>Unit tests pass for multi-table cart operations</done>
</task>

<task type="auto">
  <name>Task 3: Create OrderingPage scaffold and wire MapView</name>
  <files>frontend/salle/src/pages/Ordering/OrderingPage.tsx, frontend/salle/src/pages/Map/MapView.tsx</files>
  <action>
    - In `OrderingPage.tsx`: Use `useParams` to get `id`. Display "Ordering for Table {id}" and a "Back to Map" button.
    - In `MapView.tsx`: Wrap the table elements or add a click handler that uses `useNavigate` to go to `/tables/{id}/order`.
  </action>
  <verify>
    <automated>grep -r "useParams" frontend/salle/src/pages/Ordering/OrderingPage.tsx</automated>
  </verify>
  <done>Clicking a table in the map navigates to the ordering page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL -> UI | `tableId` from URL must be validated before use in API calls |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | S | Routing | mitigate | Validate `tableId` is a number and exists in DB (checked via API in next plans) |
</threat_model>

<verification>
- Run `npm run build` in Salle frontend to ensure no type errors.
- Run `npm test` to ensure store logic is solid.
</verification>

<success_criteria>
- Navigation between Map and Order flow is seamless.
- State is preserved per table correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/12-order-taking-frontend/12-01-SUMMARY.md`
</output>
