---
phase: 12-order-taking-frontend
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified: [
  "frontend/salle/src/pages/Ordering/components/FloatingCart.tsx",
  "frontend/salle/src/pages/Ordering/components/OrderReview.tsx",
  "frontend/salle/src/pages/Ordering/OrderingPage.tsx"
]
autonomous: true
requirements: [ORD-FE-04, ORD-FE-05]

must_haves:
  truths:
    - "Floating cart shows current item count and subtotal"
    - "OrderReview drawer displays a full itemized list for confirmation"
    - "Submitting an order sends a POST request to /api/commandes/"
    - "On success, a confirmation animation plays and redirects back to Map"
  artifacts:
    - path: "frontend/salle/src/pages/Ordering/components/FloatingCart.tsx"
      provides: "Sticky action bar for cart access"
    - path: "frontend/salle/src/pages/Ordering/components/OrderReview.tsx"
      provides: "Final validation step (Mandatory)"
  key_links:
    - from: "OrderReview.tsx"
      to: "POST /api/commandes/"
      via: "axios.post with nested lines"
---

<objective>
Implement the cart summary, final review drawer, and order submission flow.

Purpose: Finalize the ordering process and persist the order to the backend.
Output: Floating cart summary, slide-up review drawer, and API integration for order creation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-order-taking-frontend/12-RESEARCH.md
@DESIGN.md
@frontend/salle/src/pages/Ordering/OrderingPage.tsx
@frontend/salle/src/pages/Ordering/store/useOrderStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FloatingCart summary bar</name>
  <files>frontend/salle/src/pages/Ordering/components/FloatingCart.tsx</files>
  <action>
    - Create a sticky/fixed bar at the bottom of the viewport using Tailwind `fixed bottom-0`.
    - Display "X items" and "Total: {amount} DH".
    - Only show if cart is not empty. Use `framer-motion` `AnimatePresence` for smooth entry/exit.
    - Clicking the bar opens the `OrderReview` drawer.
  </action>
  <verify>
    <automated>grep "AnimatePresence" frontend/salle/src/pages/Ordering/components/FloatingCart.tsx</automated>
  </verify>
  <done>Floating cart summary is visible when items are selected</done>
</task>

<task type="auto">
  <name>Task 2: Implement OrderReview slide-up drawer</name>
  <files>frontend/salle/src/pages/Ordering/components/OrderReview.tsx</files>
  <action>
    - Create a slide-up drawer/overlay using `framer-motion`.
    - List all items in the current table's cart with quantities and subtotal.
    - Implement a "Confirm Order" button that triggers the submission.
    - Follow DESIGN.md for modal/drawer transitions (scale entry, blur backdrop).
  </action>
  <verify>
    <automated>grep "Confirm Order" frontend/salle/src/pages/Ordering/components/OrderReview.tsx</automated>
  </verify>
  <done>Mandatory review drawer allows final confirmation before submission</done>
</task>

<task type="auto">
  <name>Task 3: Implement order submission and success handling</name>
  <files>frontend/salle/src/pages/Ordering/OrderingPage.tsx, frontend/salle/src/pages/Ordering/components/OrderReview.tsx</files>
  <action>
    - Implement the `submitOrder` function in `OrderingPage` or `useOrderStore`:
      - Map cart items to the `ligne_commandes` format required by the API.
      - Send `POST /api/commandes/` with `table_id` and `lignes`.
    - On Success:
      - Clear the table's cart in the store.
      - Play a "Success" animation (e.g., checkmark pop).
      - Redirect to `/` (MapView) after 1.5 seconds.
    - On Error:
      - Display a clear error toast or message.
      - Keep the cart intact so the user can retry.
  </action>
  <verify>
    <automated>cd frontend/salle; npm run build</automated>
  </verify>
  <done>Orders are successfully persisted to the backend and UI gives clear feedback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> API | Order data is sent over the network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | T | Order submission | mitigate | **CRITICAL**: Backend recalculates prices (Phase 11); UI total is for display only. |
| T-12-04 | R | Order submission | mitigate | Log submission results for audit (handled by Django server logs) |
</threat_model>

<verification>
- Perform a full end-to-end flow: Select table -> Add items -> Review -> Confirm -> Success -> Map.
- Verify table status changes on Map after order (Phase 11 signal check).
</verification>

<success_criteria>
- Reliable order persistence.
- High-quality "Eco-Fresh" feedback loop for the user.
</success_criteria>

<output>
After completion, create `.planning/phases/12-order-taking-frontend/12-03-SUMMARY.md`
</output>
