## Phase 15 Checkpoint

Automated execution for `15-03-PLAN.md` is complete. `gsd-next` should stop here until the manual UAT below is performed.

### Completed

- `backend/apps/commandes/tasks.py` broadcasts `line_launched`.
- `backend/apps/commandes/tests/test_orchestrator.py -v` passed with `8 passed`.
- `celery-worker` is running and reachable via `celery -A tastify_backend inspect ping`.
- Smoke ETA task `cffb63d4-0391-4334-9b37-cefccd0b3979` executed successfully with `{'skipped': 'line_deleted', 'ligne_id': 999999}`.

### Blocking Manual UAT

1. Open a CUISINIER session on `http://localhost:3000` and inspect the staff websocket frames.
2. Create a multi-item order from the SERVEUR flow with short prep times.
3. Confirm `line_launched` websocket frames arrive with:
   - `ligne_id`
   - `commande_id`
   - `plat_nom`
   - `heure_lancement`
   - `heure_fin_estimee`
4. Confirm KDS behavior is consistent with the launch frames.
5. If you edit an in-flight order, confirm the worker logs show task revocation for the stale ETA.

### Resume Signal

Reply with `approved` if the manual UAT passes, or describe the issue you observed.
