---
context: default
phase: ui-ux-audit-refonte-backoffice
task: 4
total_tasks: 5
status: in_progress
last_updated: "2026-05-29T18:00:00+01:00"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_No blocking constraints discovered this session._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `replace_all` on generic tokens | `text-white` was replaced globally in SallePage — only ENCAISSEMENT badge used it on a colored bg. Check intent before sweeping semantic class names. | advisory | When doing replace_all on a class that has multiple valid uses (text-white, text-black), grep first to see all uses in context before replacing |
| Undefined CSS token usage | `bg-surface-main` was used in ReservationsPage footer — not defined in index.css @theme; caught during sweep, fixed to `bg-surface-container-lowest` | advisory | After any token replacement, grep for `surface-main`, `bg-main`, etc. to catch undefined tokens before build |

<current_state>
Ad-hoc UI/UX audit + refactor of the backoffice app (`app/frontend/backoffice-app/`).
Three phases committed (A: chrome, B+C waves 1+2: de-decorate + tokenize).
**Currently paused at Phase D: French language lock.**
The build has NOT been run yet. Run it as the first action after Phase D.
</current_state>

<completed_work>

## Session work (2026-05-29)

Commits this session:
- `47a87bc` — Phase A: Sidebar/Topbar white→dark surface tokens (P0 contrast fix)
- `a8824e5` — Phase B+C Wave 1: index.css + 10 files de-decorated + tokenized
- `ecaa863` — Phase B+C Wave 2: 7 more page files de-decorated + tokenized

**What was done:**
- `src/index.css`: removed `shadow-lg shadow-primary/10` from `.btn-primary`; rewrote `pulse-border-error` to pulse hard outline instead of box-shadow glow
- Removed all `backdrop-blur-*`, `blur-[100px]`/`blur-[50px]` decorative blobs, `shadow-2xl`/`shadow-xl`/colored glow shadows, `animate-bounce`, `shadow-[...]` custom shadows across all pages
- Replaced 165 hardcoded hex literals (`#080808`, `#0A0A0A`, `#0F0F0F`, `#111111`, `#121212`, `#1A1A1A`, `#222222`, `#333333`, `#444444`, `#888888`) with `@theme` tokens
- Fixed `text-white`/`text-black` → `text-on-surface`/`text-on-primary`/`text-on-error` semantic tokens
- Removed `bg-surface-main` undefined token → `bg-surface-container-lowest` (ReservationsPage)
- Removed `animate-in slide-in-from-bottom-20 fade-in duration-700` decorative slide-in (OrderingPage)
- Fixed `bg-[radial-gradient(...)]` radial gradient on OrderingPage menu panel → `bg-background`
- Preserved: KDS white "thermal-paper" ticket bg, QR code white card (both functional/intentional)
- Dashboard: fixed `bg-white` status chip → `bg-surface-container-low`, removed `hover:scale` on KPI cards
</completed_work>

<remaining_work>

### Task 4: Phase D — French language lock

Apply to **6 files**:

**`app/frontend/backoffice-app/src/pages/Staff/OrderingPage.tsx`**
- Line ~258: `placeholder="SEARCH..."` → `placeholder="RECHERCHER..."`

**`app/frontend/backoffice-app/src/pages/Staff/KdsPage.tsx`**
- `'Units Actives'` → `'Unités Actives'`
- `'Status'` → `'Statut'`
- `'LIVE SYNC'` → `'SYNC EN DIRECT'`

**`app/frontend/backoffice-app/src/layouts/Sidebar.tsx`**
- Line ~127: `"System Ready"` → `"Système Prêt"`
- Note: `"Intelligent Restaurant OS"` (line 92) is brand tagline — leave as-is or change to `"OS Restauration Intelligent"` (confirm with user if unsure)

**`app/frontend/backoffice-app/src/pages/Avis/AvisPage.tsx`**
- `"Global Satisfaction"` → `"Satisfaction Globale"`

**`app/frontend/backoffice-app/src/pages/System/MaintenancePage.tsx`** (mostly English — full heading/label sweep):
- `"System Health"` → `"Santé Système"`
- `"Real-time telemetry and module status"` → `"Télémétrie temps réel et état des modules"`
- `"Export Logs"` → `"Exporter Logs"`
- `"Manual Sync"` → `"Sync Manuelle"`
- `"Kitchen Core"` → `"Noyau Cuisine"`
- `"Online"` → `"En Ligne"`
- `"Floor Comm"` → `"Comm. Salle"`
- `"Degraded"` → `"Dégradé"`
- `"System Event Log"` → `"Journal Événements"`
- `"Environment"` → `"Environnement"`
- `"Last Boot"` → `"Dernier Démarrage"`
- `"Technical Support"` → `"Support Technique"`
- `"For critical outages..."` message → translate
- Keep mock data values as-is (lat/uptime numbers, log messages, IDs)

**`app/frontend/backoffice-app/src/pages/Staff/DeliveryHubPage.tsx`** (whole page in English):
- `"DELIVERY HUB"` → `"PÔLE LIVRAISON"`
- `"SYSTEMS NOMINAL • 14 ACTIVE"` → `"SYSTÈMES NOMINAUX • 14 ACTIFS"`
- `placeholder="Search orders..."` → `placeholder="Rechercher commandes..."`
- KPI labels: `"OFF-PREMISE REVENUE"` → `"CA LIVRAISON"`, `"AVG. PREP TIME"` → `"PRÉP. MOY."`, `"DRIVER SATISFACTION"` → `"SAT. LIVREURS"`, `"CANCELLATION RATE"` → `"TAUX ANNULATION"`, `"Nominal"` → `"Nominal"` (already ambiguous; keep)
- Table headers: `"Time"` → `"Heure"`, `"Channel"` → `"Canal"`, `"Order ID"` → `"N° Commande"`, `"Items"` → `"Articles"`, `"Status / Courier"` → `"Statut / Livreur"`, `"Value"` → `"Montant"`, `"Action"` → `"Action"` (same)
- Tab labels: `"All Active (14)"` → `"Tous Actifs (14)"`, `"Preparing (6)"` → `"En Prép. (6)"`, `"En Route (5)"` → `"En Route (5)"` (already FR)
- `"Active Delivery Radius"` → `"Zone de Livraison"`
- `"LIVE MONITORING"` → `"SURVEILLANCE LIVE"`
- `"Density"` → `"Densité"`, `"Traffic"` → `"Trafic"`
- `"Courier Wait List"` → `"File d'Attente Livreurs"`
- `"Audit Dispatch Log"` → `"Journal Dispatch"`
- Status values in table: `"PREPARING"` → `"EN PRÉP."`, `"READY"` → `"PRÊT"`, `"EN ROUTE"` already FR

Then commit: `refactor(ui): lock UI language to French (Phase D)`

### Task 5: Build verification + CLAUDE.md sync

```bash
cd app/frontend/backoffice-app && npm run build
```
Must pass (TypeScript + Vite). If errors, investigate and fix before proceeding.

Then:
1. Update `docs/brain/02_Journal/CHANGELOG.md` — add entry for all 4 commits (47a87bc, a8824e5, ecaa863, Phase D commit)
2. `python scripts/update_dashboard.py`
3. Log in `docs/brain/03_Architecture/QUIRKS.md`:
   - `surface-main` was an undefined CSS token; now replaced with `surface-container-lowest`
   - KDS white ticket-paper (`bg-white` on ticket item list) is intentional — do not tokenize it
</remaining_work>

<decisions_made>

- **Direction locked:** Dark tactical theme (`DESIGN.md`) is the source of truth. No light Stripe/Shopify pivot.
- **KDS white ticket surfaces preserved:** `bg-white` on the KDS ticket item list is an intentional skeuomorphic kitchen-ticket metaphor — not drift. Do not remove it.
- **QR code white card preserved:** `p-10 bg-white rounded-[3rem]` in OrderingPage payment modal is functionally required for QR code contrast/scannability.
- **Sidebar brand tagline:** `"Intelligent Restaurant OS"` (line 92 Sidebar.tsx) — potentially a brand choice. `"System Ready"` (line 127) is clearly operational UI copy and must become `"Système Prêt"`.
- **B+C combined per file:** Phases B (remove effects) and C (tokenize hex) were applied together per file for efficiency, with a single atomic commit per wave.
- **Mock data content not translated:** Hardcoded courier names, log messages, mock order data left as-is — they're content/placeholder data, not UI labels.
</decisions_made>

<blockers>
None. No external dependencies.
</blockers>

## Required Reading (in order)

1. `C:/Users/mehdi/.claude/plans/ui-ux-audit-radiant-codd.md` — The full audit report + phased plan; defines the scope and rationale for every change
2. `app/frontend/backoffice-app/src/index.css` — The `@theme` token definitions; know the token names before editing files
3. `DESIGN.md` (repo root) — The source-of-truth design system (dark tactical theme rules, contrast requirements, no-blur/no-shadow mandate)

## Infrastructure State

- Docker: not required for frontend-only changes
- Dev server: `app/frontend/backoffice-app/` runs on Vite (`npm run dev`) — optional for visual check; required step is `npm run build`
- Backend: not involved in this work

## Pre-Execution Critique Required

_Not applicable — design phase is complete (plan is at `.claude/plans/ui-ux-audit-radiant-codd.md`), execution is in progress._

<context>
Session was a large UI/UX audit + refactor of the backoffice frontend to align with DESIGN.md (dark tactical theme). The audit identified 2 P0 issues (white sidebar/topbar on dark-token text = broken contrast), 5 P1 issues (forbidden effects, hardcoded hex, FR/EN mixing, missing states), and 6 P2 polish issues.

Three of four quick-win phases were completed and committed this session. Context ran out during Phase D. The plan is structured (audit doc at `.claude/plans/ui-ux-audit-radiant-codd.md`) and Phase D is a simple string search-and-replace sweep across 6 files. Phase E (structural component extraction) is deliberately deferred.
</context>

<next_action>
Start with Phase D: open `app/frontend/backoffice-app/src/pages/Staff/OrderingPage.tsx` and replace `placeholder="SEARCH..."` with `placeholder="RECHERCHER..."`, then continue through the other 5 files listed in `<remaining_work>`, commit, then run `npm run build` in `app/frontend/backoffice-app/`.
</next_action>
