# Plan 19-03: Stock Adjustments & Alerts

## 1. Goal
Provide a fast way to adjust stock levels and visually highlight low-stock items.

## 2. Tasks
- [ ] Create `app/frontend/backoffice/src/pages/Stock/StockAdjustmentModal.tsx`.
- [ ] Implement color-coding logic in `IngredientList` and `IngredientMobileCard` based on thresholds (Amber/Terracotta).
- [ ] Add the "Alertes uniquement" toggle to the page header.
- [ ] Connect the adjustment modal to the `PATCH /api/stock/ingredients/{id}/` endpoint.

## 3. Verification
- [ ] Verify adjusting stock reflects immediately in the list.
- [ ] Verify that items below threshold are highlighted in red (Terracotta).
- [ ] Verify that items near threshold are highlighted in amber.
- [ ] Verify that "Alertes uniquement" filter works correctly.
