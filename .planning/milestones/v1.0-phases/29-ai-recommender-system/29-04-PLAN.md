---
phase: 29
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: ["app/frontend/portail/src/api/menu.ts"]
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Navigate to the Menu page. See a list of available dishes (Plats)."
    - "Select a dish from the Menu page. A 'Recommandé pour vous' or 'Recommendations' section appears."
    - "Select a dish that has no similar items yet. Recommendations still appear."
  artifacts:
    - path: "app/frontend/portail/src/api/menu.ts"
      provides: "Menu API client"
  key_links: []
---

<objective>
Fix the Vite import-analysis failure in `menu.ts` that prevents the Portail frontend from compiling and loading.

Purpose: The relative path `../../../shared/auth/axiosInstance` crashes the app in the Docker environment. We must replace it with the `@shared` alias.
Output: Fixed import statement in `menu.ts`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/frontend/portail/src/api/menu.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix relative import path in menu.ts</name>
  <files>app/frontend/portail/src/api/menu.ts</files>
  <action>Change the import of `axiosInstance` from the relative path `../../../shared/auth/axiosInstance` to the alias path `@shared/auth/axiosInstance`.</action>
  <verify>
    <automated>grep -q "@shared/auth/axiosInstance" app/frontend/portail/src/api/menu.ts ; npm --prefix app/frontend/portail run build</automated>
  </verify>
  <done>The import uses the @shared alias and the frontend build succeeds without import analysis errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | Client requests menu and recommendations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Tampering | app/frontend/portail/src/api/menu.ts | accept | Static frontend change, no new security boundaries introduced. |
</threat_model>

<verification>
- The Portail frontend builds successfully.
</verification>

<success_criteria>
- The Vite import-analysis error is resolved.
- `app/frontend/portail/src/api/menu.ts` uses the `@shared` alias for `axiosInstance`.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ai-recommender-system/29-04-SUMMARY.md`
</output>
