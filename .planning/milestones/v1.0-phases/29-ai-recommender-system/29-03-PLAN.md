---
phase: 29-ai-recommender-system
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - app/frontend/portail/src/api/menu.ts
  - app/frontend/portail/src/components/menu/RecommendationList.tsx
  - app/frontend/portail/src/pages/Menu/MenuPage.tsx
  - app/frontend/portail/src/App.tsx
autonomous: true
requirements:
  - REQ-29-3
must_haves:
  truths:
    - User can navigate to the Menu page
    - User can see a list of Plats
    - Selecting a Plat displays its recommendations
  artifacts:
    - path: app/frontend/portail/src/pages/Menu/MenuPage.tsx
      provides: Menu browsing UI
    - path: app/frontend/portail/src/components/menu/RecommendationList.tsx
      provides: Recommended for you section
  key_links:
    - from: app/frontend/portail/src/components/menu/RecommendationList.tsx
      to: /api/menu/plats/<id>/recommendations/
      via: axiosInstance
---

<objective>
Integrate the AI Recommender System into the Portail Client React app.
Purpose: Allow customers to browse the menu and see AI-driven dish suggestions.
Output: A new Menu page with a `RecommendationList` component that displays "Recommended for you" items.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/frontend/portail/src/App.tsx

<interfaces>
Types needed for frontend (to be created in `app/frontend/portail/src/api/menu.ts`):
```typescript
export interface Plat {
  id: number;
  nom: string;
  description: string;
  prix: string;
  image: string | null;
  categorie: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API Client for Menu</name>
  <files>app/frontend/portail/src/api/menu.ts</files>
  <action>
    Create the API client methods for fetching the menu and recommendations.
    1. Define `Plat` interface.
    2. Export `fetchPlats()` calling `GET /api/menu/plats/`.
    3. Export `fetchRecommendations(platId: number)` calling `GET /api/menu/plats/${platId}/recommendations/`.
  </action>
  <verify>
    <automated>npm run typecheck --prefix app/frontend/portail</automated>
  </verify>
  <done>Types and fetch methods are exported without type errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RecommendationList Component</name>
  <files>app/frontend/portail/src/components/menu/RecommendationList.tsx, app/frontend/portail/src/components/menu/RecommendationList.test.tsx</files>
  <behavior>
    - Test 1: Renders a loading state initially.
    - Test 2: Renders a list of Plats when data is loaded successfully.
    - Test 3: Displays nothing or a fallback message on error/empty results.
  </behavior>
  <action>
    Create a React component that takes a `platId` prop.
    1. On mount or `platId` change, call `fetchRecommendations(platId)`.
    2. Display the results in a horizontal scrollable list or simple grid with the title "Recommandé pour vous".
    3. Ensure to handle loading and error states properly using existing UI patterns (e.g., standard Tailwind classes).
  </action>
  <verify>
    <automated>npm run test --prefix app/frontend/portail -- --run --testNamePattern="RecommendationList"</automated>
  </verify>
  <done>Component correctly fetches and renders recommendations.</done>
</task>

<task type="auto">
  <name>Task 3: Menu Page and Routing</name>
  <files>app/frontend/portail/src/pages/Menu/MenuPage.tsx, app/frontend/portail/src/App.tsx</files>
  <action>
    1. Create `MenuPage.tsx` that fetches all `Plats` via `fetchPlats()` and lists them.
    2. Add state for `selectedPlatId`. When a user clicks a plat, set this state and display the `RecommendationList` below or alongside the selected plat.
    3. Update `App.tsx` to include a new route `<Route path="/menu" element={<MenuPage />} />` inside `ProtectedClientShell`.
    4. Add a navigation link "Menu" in the `ProtectedClientShell` header, next to "Portail Client".
  </action>
  <verify>
    <automated>npm run build --prefix app/frontend/portail</automated>
  </verify>
  <done>Menu page is accessible at `/menu` and correctly incorporates the recommendations component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> UI | XSS via Plat names or descriptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03-01 | Spoofing | `MenuPage` | accept | React automatically escapes string bindings in JSX. |
</threat_model>

<verification>
- `npm run build` succeeds for portail
- Frontend test suite passes
</verification>

<success_criteria>
- The "Recommended for you" feature is fully interactive in the client app.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ai-recommender-system/29-03-SUMMARY.md`
</output>