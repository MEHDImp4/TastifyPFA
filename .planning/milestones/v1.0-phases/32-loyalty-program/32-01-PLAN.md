---
phase: 32-loyalty-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/backend/apps/paiements/models.py",
  "app/backend/apps/paiements/views.py",
  "app/backend/apps/loyalty/models.py",
  "app/backend/apps/loyalty/serializers.py",
  "app/backend/apps/loyalty/views.py",
  "app/backend/apps/loyalty/signals.py",
  "app/backend/apps/loyalty/apps.py",
  "app/backend/tastify_backend/settings/base.py",
  "app/backend/tastify_backend/api_router.py"
]
autonomous: true
requirements: [LOY-01, LOY-02, LOY-03]
must_haves:
  truths:
    - "Un paiement COMPLETE avec un client associé génère des points"
    - "L'historique des transactions de fidélité est enregistré"
    - "Le solde de points est consultable via l'API"
  artifacts:
    - path: "app/backend/apps/loyalty/models.py"
      provides: "LoyaltyProfile, LoyaltyTransaction, Reward models"
    - path: "app/backend/apps/loyalty/signals.py"
      provides: "Automatic point awarding on payment"
  key_links:
    - from: "app/backend/apps/paiements/models.py"
      to: "app/backend/apps/loyalty/signals.py"
      via: "post_save signal"
      pattern: "post_save\\.connect.*award_points"
---

<objective>
Mettre en place l'infrastructure backend pour le programme de fidélité.
Ceci inclut les modèles de données, la logique d'attribution automatique des points (via signaux) et les endpoints API pour la consultation et la gestion.

Sortie : Application Django 'loyalty' fonctionnelle avec API DRF.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/backend/apps/paiements/models.py
@app/backend/apps/users/models.py
@app/backend/apps/paiements/views.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Loyalty App & Models</name>
  <files>
    app/backend/apps/loyalty/models.py,
    app/backend/apps/loyalty/apps.py,
    app/backend/apps/paiements/models.py,
    app/backend/tastify_backend/settings/base.py
  </files>
  <action>
    1. Ajouter le champ 'client' (ForeignKey vers Utilisateur, null=True, blank=True) au modèle 'Paiement' dans 'app/backend/apps/paiements/models.py'.
    2. Créer l'application 'loyalty' (mkdir app/backend/apps/loyalty, init, apps.py).
    3. Définir les modèles dans 'loyalty/models.py' :
       - 'LoyaltyProfile' : user (OneToOne), points (Decimal, default=0). Ajouter une property 'tier' calculant Bronze (<500), Silver (500-1499), Gold (>=1500) selon D-03.
       - 'LoyaltyTransaction' : profile, points (Decimal), type (GAIN, DEPENSE), description, created_at.
       - 'Reward' : nom, description, points_requis (Decimal), est_actif (Bool).
    4. Enregistrer 'apps.loyalty' dans 'INSTALLED_APPS' dans 'base.py'.
    5. Générer et appliquer les migrations (makemigrations, migrate).
  </action>
  <verify>
    <automated>docker-compose exec backend python manage.py makemigrations --check</automated>
  </verify>
  <done>Modèles créés et migrations appliquées. Paiement lié au client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Point Awarding Signal</name>
  <files>app/backend/apps/loyalty/signals.py, app/backend/apps/loyalty/tests.py</files>
  <behavior>
    - Créer un Paiement COMPLETE sans client -> Aucun point attribué.
    - Créer un Paiement COMPLETE avec client -> Points attribués (1 pt pour 10 MAD, D-01).
    - Mettre à jour un Paiement de EN_ATTENTE à COMPLETE avec client -> Points attribués.
    - Vérifier qu'une LoyaltyTransaction est créée.
  </behavior>
  <action>
    1. Implémenter le signal 'post_save' sur 'Paiement' dans 'loyalty/signals.py'.
    2. Connecter le signal dans 'loyalty/apps.py' (méthode ready).
    3. La logique doit utiliser 'Decimal' pour éviter les erreurs d'arrondi. Ratio : points = montant / 10.
  </action>
  <verify>
    <automated>docker-compose exec backend pytest apps/loyalty/tests.py</automated>
  </verify>
  <done>Les points sont automatiquement crédités lors d'un paiement réussi.</done>
</task>

<task type="auto">
  <name>Task 3: Loyalty API Endpoints</name>
  <files>
    app/backend/apps/loyalty/serializers.py,
    app/backend/apps/loyalty/views.py,
    app/backend/tastify_backend/api_router.py,
    app/backend/apps/paiements/views.py
  </files>
  <action>
    1. Créer les Serializers pour LoyaltyProfile (incluant le tier), LoyaltyTransaction et Reward.
    2. Créer 'LoyaltyViewSet' avec :
       - Action 'me' (GET) : retourne le profil de l'utilisateur connecté.
       - Action 'transactions' (GET) : retourne l'historique des transactions de l'utilisateur.
    3. Créer 'RewardViewSet' (CRUD) réservé aux GERANTs.
    4. Enregistrer les routes dans 'api_router.py'.
    5. Mettre à jour 'PaiementViewSet.pay_token' et 'create' dans 'paiements/views.py' pour associer 'request.user' au Paiement si authentifié.
  </action>
  <verify>
    <automated>curl -H "Authorization: Bearer $TOKEN" http://localhost:8000/api/loyalty/me/</automated>
  </verify>
  <done>API exposée et sécurisée. Les paiements capturent désormais l'identité du client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | L'utilisateur demande ses points ou tente d'utiliser une récompense. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | Loyalty API | mitigate | Valider le solde de points côté serveur avant toute dépense (Reward redemption). |
| T-32-02 | Information Disclosure | Loyalty API | mitigate | Restreindre l'accès aux données de fidélité au propriétaire (IsOwner permission). |
| T-32-03 | Repudiation | LoyaltyTransaction | mitigate | Enregistrer chaque gain/dépense dans une table de transaction immuable. |
</threat_model>

<verification>
1. Créer un client.
2. Simuler un paiement de 500 MAD pour ce client.
3. Vérifier que le client a 50 points et est passé au tier 'SILVER' (si >= 500 MAD, 50 pts... wait, 500 pts for Silver? D-03 says 500 pts).
   Note: D-03 tiers are points based, not MAD based. 500 pts = 5000 MAD spent.
4. Vérifier l'historique via /api/loyalty/transactions/.
</verification>

<success_criteria>
- Les modèles loyalty sont créés.
- Les signaux Django attribuent les points correctement.
- L'API retourne le profil et l'historique.
- Les tests passent.
</success_criteria>

<output>
After completion, create .planning/phases/32-loyalty-program/32-01-SUMMARY.md
</output>
