---
phase: 37
plan: 01
slug: weather-integration-prediction
status: planned
---

# Plan 37-01: Weather Integration & AI Prediction

## Goal
Integrate weather data and implement the demand prediction model.

## Tasks
1.  **Weather Service**:
    - [ ] Create `apps.analytics.services.weather.WeatherService`.
    - [ ] Implement `fetch_forecast` using a mock or real API (OpenWeatherMap).
    - [ ] Add `WeatherData` model to store historical/forecasted weather.
2.  **AI Prediction Logic**:
    - [ ] Create `apps.analytics.services.predictor.DemandPredictor`.
    - [ ] Implement `train_model` using `RandomForestRegressor`.
    - [ ] Implement `predict_next_week` returns predicted quantity per dish.
3.  **Procurement Logic**:
    - [ ] Implement `get_suggested_stock` converts dish predictions to ingredient quantities using `PlatIngredient`.
4.  **Celery Integration**:
    - [ ] Scheduled task: `update_weather_and_predictions` (daily).

## Verification
- [ ] Celery task runs without error.
- [ ] Prediction returns a non-zero dictionary for active dishes.
