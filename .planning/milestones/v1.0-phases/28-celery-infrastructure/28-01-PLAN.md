---
phase: 28-celery-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/backend/requirements.txt, docker-compose.yml, app/backend/tastify_backend/settings/base.py, .env.example, app/backend/tastify_backend/celery.py]
autonomous: true
requirements: [CELERY-01, CELERY-02]
must_haves:
  truths:
    - "celery-worker and celery-beat containers start without errors"
    - "Django settings include django_celery_beat and django_celery_results"
    - "Celery broker and result backend are isolated from Channel Layer (using Redis DB 1)"
  artifacts:
    - path: "docker-compose.yml"
      provides: "celery-beat service"
    - path: "app/backend/requirements.txt"
      provides: "celery, django-celery-beat, django-celery-results"
    - path: "app/backend/tastify_backend/settings/base.py"
      provides: "Celery infrastructure configuration"
---

<objective>
Establish the robust asynchronous infrastructure for Tastify by adding Celery Beat and monitoring apps, and isolating the task broker from the real-time WebSocket layer.

Purpose: Scale long-running tasks without blocking the request/response cycle or WebSocket throughput.
Output: Operational Celery Worker and Beat containers with database-backed scheduling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docker-compose.yml
@app/backend/tastify_backend/settings/base.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Dependencies and Environment</name>
  <files>app/backend/requirements.txt, .env.example</files>
  <action>
    - Add `django-celery-beat==2.7.0` and `django-celery-results==2.5.1` to requirements.txt.
    - Update `.env.example` to include `CELERY_BROKER_URL` and `CELERY_RESULT_BACKEND` (defaulting to redis://redis:6379/1).
  </action>
  <verify>
    <automated>grep -E "django-celery-beat|django-celery-results" app/backend/requirements.txt</automated>
  </verify>
  <done>Dependencies added and environment example updated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Django Settings and Celery Entrypoint</name>
  <files>app/backend/tastify_backend/settings/base.py, app/backend/tastify_backend/celery.py</files>
  <action>
    - Add `django_celery_beat` and `django_celery_results` to `INSTALLED_APPS` in base.py.
    - Configure `CELERY_RESULT_BACKEND = 'django-db'`.
    - Configure `CELERY_CACHE_BACKEND = 'django-cache'`.
    - Set `CELERY_BEAT_SCHEDULER = 'django_celery_beat.schedulers:DatabaseScheduler'`.
    - Update `CELERY_TIMEZONE` to match `TIME_ZONE` ('Africa/Casablanca').
    - In `celery.py`, replace hardcoded `settings.dev` with `config('DJANGO_SETTINGS_MODULE', default='tastify_backend.settings.dev')`.
  </action>
  <verify>
    <automated>grep -q "django_celery_beat" app/backend/tastify_backend/settings/base.py</automated>
  </verify>
  <done>Django settings configured for database-backed scheduling and results.</done>
</task>

<task type="auto">
  <name>Task 3: Orchestrate Celery Beat in Docker</name>
  <files>docker-compose.yml</files>
  <action>
    - Add `celery-beat` service to docker-compose.yml.
    - Mirror the configuration of `celery-worker` but use command: `celery -A tastify_backend beat --loglevel=info`.
    - Ensure it depends on `db` and `redis` health.
  </action>
  <verify>
    <automated>docker-compose config | grep celery-beat</automated>
  </verify>
  <done>Celery Beat service added to orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker -> Redis | Trusted internal network (Docker bridge) |
| beat -> DB | Trusted internal network |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | D | Redis | mitigate | Isolate Celery on Redis DB 1 to prevent collision with Channels (DB 0). |
| T-28-02 | S | Beat | mitigate | Use DatabaseScheduler so scheduled tasks are managed via Django's auth-protected Admin/API. |
</threat_model>

<verification>
Check `docker-compose logs celery-worker` and `celery-beat` for "beat: Starting..." and worker registration.
</verification>

<success_criteria>
1. `docker-compose ps` shows celery-worker and celery-beat as "Up".
2. Django Admin (if accessed) shows "Periodic Tasks" section.
</success_criteria>

<output>
After completion, create `.planning/phases/28-celery-infrastructure/28-01-SUMMARY.md`
</output>
