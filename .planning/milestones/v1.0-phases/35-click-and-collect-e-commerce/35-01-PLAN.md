---
phase: 35
plan: 01
slug: click-and-collect-backend
status: planned
---

# Plan 35-01: Click & Collect Backend Foundation

## Goal
Update the `commandes` model and API to support takeaway orders.

## Tasks
1.  **Model Update**:
    - [ ] Add `type` field to `Commande` (SUR_PLACE, EMPORTER).
    - [ ] Make `table` field nullable in `Commande`.
    - [ ] Add `client_nom` field to `Commande` for identification.
    - [ ] Run migrations.
2.  **API Enhancement**:
    - [ ] Update `CommandeSerializer` to handle the new fields.
    - [ ] Update `CommandeViewSet.get_queryset` to include takeaway orders in kitchen scope.
3.  **Real-time**:
    - [ ] Ensure takeaway orders are broadcasted to KDS.
4.  **Tests**:
    - [ ] Test creation of EMPORTER order without a table.
    - [ ] Verify KDS queryset includes EMPORTER orders.

## Verification
- [ ] Pytest suite for `commandes` passes.
