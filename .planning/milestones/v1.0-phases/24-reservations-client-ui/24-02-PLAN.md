---
phase: 24-reservations-client-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/frontend/portail/package.json
  - app/frontend/portail/vitest.config.ts
  - app/frontend/portail/src/test/setup.ts
  - app/frontend/portail/src/App.tsx
  - app/frontend/portail/src/api/reservations.ts
  - app/frontend/portail/src/pages/Reservations/WizardContext.tsx
  - app/frontend/portail/src/pages/Reservations/ReservationWizardShell.tsx
autonomous: true
requirements:
  - REQ-24-B
  - REQ-24-F

must_haves:
  truths:
    - "portail installs and resolves react-router-dom@6.30.3 and framer-motion@12.38.0"
    - "portail vitest test suite runs with jsdom environment"
    - "App.tsx uses BrowserRouter with v7 future flags and route-based auth guard"
    - "WizardContext exports WizardProvider and useWizard with correct TypeScript types"
    - "WizardContext.test.tsx passes: setDateTime and setTable update state; reset clears it"
    - "portail TypeScript build (tsc -b) passes with zero errors"
  artifacts:
    - path: "app/frontend/portail/package.json"
      provides: "react-router-dom, framer-motion, vitest deps"
      contains: "react-router-dom"
    - path: "app/frontend/portail/vitest.config.ts"
      provides: "vitest jsdom config"
      contains: "environment: 'jsdom'"
    - path: "app/frontend/portail/src/test/setup.ts"
      provides: "jest-dom matchers"
      contains: "@testing-library/jest-dom"
    - path: "app/frontend/portail/src/App.tsx"
      provides: "BrowserRouter + route-based auth"
      contains: "BrowserRouter"
    - path: "app/frontend/portail/src/pages/Reservations/WizardContext.tsx"
      provides: "WizardProvider + useWizard hook"
      contains: "WizardProvider"
    - path: "app/frontend/portail/src/api/reservations.ts"
      provides: "fetchAvailableTables + createReservation API functions"
      contains: "fetchAvailableTables"
    - path: "app/frontend/portail/src/pages/Reservations/ReservationWizardShell.tsx"
      provides: "nested Routes shell wrapping WizardProvider"
      contains: "ReservationWizardShell"
  key_links:
    - from: "App.tsx"
      to: "ReservationWizardShell"
      via: "Route path='/reservations/*'"
      pattern: "ReservationWizardShell"
    - from: "ReservationWizardShell"
      to: "WizardProvider"
      via: "wraps children in context"
      pattern: "WizardProvider"
    - from: "api/reservations.ts"
      to: "@shared/auth/axiosInstance"
      via: "import axiosInstance"
      pattern: "axiosInstance\\.get.*available_tables"
---

<objective>
Install frontend dependencies, scaffold the portail vitest config, wire BrowserRouter routing into App.tsx, create WizardContext with its test, create the API service module, and create ReservationWizardShell. This wave 1 plan runs in parallel with 24-01 (backend).

Purpose: Wave 2 (Plan 03) cannot build wizard page components until routing, context, and the test framework are in place. This plan establishes all scaffolding contracts.
Output: portail builds (tsc -b), vitest runs, routing works end-to-end from /login to /reservations/new.
</objective>

<execution_context>
@C:/Users/mehdi/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mehdi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-reservations-client-ui/24-RESEARCH.md
@.planning/phases/24-reservations-client-ui/24-PATTERNS.md
@DESIGN.md

<interfaces>
<!-- Current portail package.json (lines 1-28 — entire file): -->
```json
{
  "name": "tastify-portail-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "lucide-react": "^1.11.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zustand": "^5.0.12"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/node": "^25.6.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
```

<!-- Current portail App.tsx (lines 1-76 — entire file): -->
```typescript
import { useEffect, useState } from 'react'
import { AuthBootstrap } from '@shared/auth/AuthBootstrap'
import { useAuthStore } from '@shared/auth/useAuthStore'
import Login from '@shared/auth/Login'
import axiosInstance from '@shared/auth/axiosInstance'
import { CLIENT_ROLES, isRoleAllowed } from '@shared/auth/roleAccess'
import logo from '@shared/assets/logo-public.svg'
import { AppErrorBoundary } from '@shared/ui/AppErrorBoundary'

function App() { ... /* conditional rendering, no BrowserRouter */ }
export default App
```

<!-- Backoffice App.tsx routing pattern (analog): -->
```typescript
import { BrowserRouter, Routes, Route, Navigate, useNavigate } from 'react-router-dom'
// BrowserRouter future flags:
// future={{ v7_startTransition: true, v7_relativeSplatPath: true }}
```

<!-- Backoffice vitest.config.ts (exact content to mirror): -->
```typescript
import { defineConfig, mergeConfig } from 'vitest/config'
import viteConfig from './vite.config'

export default mergeConfig(
  viteConfig,
  defineConfig({
    test: {
      globals: true,
      environment: 'jsdom',
      setupFiles: './src/test/setup.ts',
    },
  }),
)
```

<!-- WizardState type (from RESEARCH.md): -->
```typescript
interface WizardState {
  date: string           // 'YYYY-MM-DD'
  heure_debut: string    // 'HH:MM'
  heure_fin: string      // 'HH:MM'
  nombre_personnes: number
  selectedTable: Table | null
}
```

<!-- Table type (from @shared/types/tables): -->
```typescript
interface Table {
  id: number; numero: number; capacite: number;
  statut: TableStatus;
  pos_x: number; pos_y: number;
  est_active: boolean; created_at: string; updated_at: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, vitest config, and test setup scaffold</name>
  <files>
    app/frontend/portail/package.json,
    app/frontend/portail/vitest.config.ts,
    app/frontend/portail/src/test/setup.ts
  </files>
  <read_first>
    - app/frontend/portail/package.json (read the current file — 28 lines — before editing)
    - app/frontend/backoffice/vitest.config.ts (read the full analog config to copy verbatim)
    - app/frontend/backoffice/src/test/setup.ts (read the single-line setup to copy verbatim)
    - app/frontend/backoffice/package.json (read to confirm exact package versions: react-router-dom ^6.30.3, framer-motion ^12.38.0, vitest ^4.1.5, @testing-library/react ^16.3.2, @testing-library/jest-dom ^6.9.1, jsdom ^29.1.0, @types/react-router-dom ^5.3.3)
  </read_first>
  <action>
Step A — Install packages. Run from Windows PowerShell:
```powershell
cd app/frontend/portail ; npm install react-router-dom@^6.30.3 framer-motion@^12.38.0 ; npm install --save-dev vitest@^4.1.5 "@testing-library/react@^16.3.2" "@testing-library/jest-dom@^6.9.1" jsdom@^29.1.0 "@types/react-router-dom@^5.3.3"
```

Step B — Edit package.json: add `"test": "vitest"` to the `scripts` block. The existing `dev`, `build`, `preview` scripts stay unchanged.

Step C — Create `app/frontend/portail/vitest.config.ts` (copy from backoffice verbatim):
```typescript
import { defineConfig, mergeConfig } from 'vitest/config'
import viteConfig from './vite.config'

export default mergeConfig(
  viteConfig,
  defineConfig({
    test: {
      globals: true,
      environment: 'jsdom',
      setupFiles: './src/test/setup.ts',
    },
  }),
)
```

Step D — Create `app/frontend/portail/src/test/setup.ts` (copy from backoffice verbatim):
```typescript
import '@testing-library/jest-dom'
```
  </action>
  <verify>
    <automated>cd app/frontend/portail ; npx vitest run --reporter=verbose 2>&amp;1 | Select-String -Pattern "no test files|passed|failed"</automated>
  </verify>
  <done>
    - `grep -l "react-router-dom" app/frontend/portail/package.json` returns the file
    - `grep -l "framer-motion" app/frontend/portail/package.json` returns the file
    - `grep -l "vitest" app/frontend/portail/package.json` returns the file
    - `app/frontend/portail/vitest.config.ts` exists and contains `environment: 'jsdom'`
    - `app/frontend/portail/src/test/setup.ts` exists and contains `@testing-library/jest-dom`
    - `npx vitest run` exits 0 (no test files yet — that is expected at this step)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App.tsx routing + WizardContext + API service + WizardShell</name>
  <files>
    app/frontend/portail/src/App.tsx,
    app/frontend/portail/src/api/reservations.ts,
    app/frontend/portail/src/pages/Reservations/WizardContext.tsx,
    app/frontend/portail/src/pages/Reservations/WizardContext.test.tsx,
    app/frontend/portail/src/pages/Reservations/ReservationWizardShell.tsx
  </files>
  <read_first>
    - app/frontend/portail/src/App.tsx (read the full current file — 76 lines — before rewriting)
    - app/frontend/backoffice/src/App.tsx (read lines 1-120 for BrowserRouter + LoginRoute + ProtectedShell patterns)
    - app/frontend/shared/auth/roleAccess.ts (confirm CLIENT_ROLES export name and isRoleAllowed signature)
    - app/frontend/shared/auth/useAuthStore.ts (confirm isAuthenticated, user, clearAuth exports)
    - app/frontend/shared/auth/axiosInstance.ts (confirm default export name)
    - app/frontend/shared/types/tables.ts (confirm Table interface fields)
  </read_first>
  <behavior>
    Test: WizardProvider setDateTime updates date, heure_debut, heure_fin, nombre_personnes in state
    Test: WizardProvider setTable updates selectedTable in state
    Test: WizardProvider reset returns state to initial values (date: '', nombre_personnes: 1, selectedTable: null)
    Test: useWizard throws "useWizard must be used inside WizardProvider" when called outside provider
  </behavior>
  <action>
**File 1: app/frontend/portail/src/App.tsx** — full replacement.

Rewrite App.tsx to use BrowserRouter routing, preserving all existing auth logic (logout, role guard, kickMessage). The placeholder welcome card JSX is REMOVED — replaced by routed pages.

```typescript
import { useEffect, useState } from 'react'
import { BrowserRouter, Routes, Route, Navigate, useNavigate } from 'react-router-dom'
import { AuthBootstrap } from '@shared/auth/AuthBootstrap'
import { useAuthStore } from '@shared/auth/useAuthStore'
import Login from '@shared/auth/Login'
import axiosInstance from '@shared/auth/axiosInstance'
import { CLIENT_ROLES, isRoleAllowed } from '@shared/auth/roleAccess'
import { AppErrorBoundary } from '@shared/ui/AppErrorBoundary'
import { ReservationWizardShell } from './pages/Reservations/ReservationWizardShell'

const ClientLoginRoute = () => {
  const { isAuthenticated, user, clearAuth } = useAuthStore()
  const navigate = useNavigate()
  const [kickMessage, setKickMessage] = useState<string | undefined>()

  useEffect(() => {
    if (isAuthenticated) {
      if (user?.role && !isRoleAllowed(user.role, CLIENT_ROLES)) {
        clearAuth()
        setKickMessage("Ce compte est réservé à l'espace staff.")
        return
      }
      navigate('/', { replace: true })
    }
  }, [clearAuth, isAuthenticated, navigate, user?.role])

  return (
    <Login
      onSuccess={() => navigate('/')}
      allowedRoles={CLIENT_ROLES}
      appLabel="Portail Client"
      appDescription="Accès réservé aux clients du restaurant"
      deniedMessage="Ce compte est réservé à l'espace staff."
      variant="client"
      initialError={kickMessage}
    />
  )
}

const ProtectedClientShell = () => {
  const { isAuthenticated, user, clearAuth } = useAuthStore()
  const navigate = useNavigate()

  const handleLogout = async () => {
    try {
      await axiosInstance.post('/users/logout/')
    } catch (err) {
      console.error('Logout failed', err)
    } finally {
      clearAuth()
    }
  }

  useEffect(() => {
    if (!isAuthenticated || (user?.role && !isRoleAllowed(user.role, CLIENT_ROLES))) {
      navigate('/login', { replace: true })
    }
  }, [isAuthenticated, navigate, user?.role])

  return (
    <main className="min-h-screen bg-background text-foreground font-sans">
      <header className="flex items-center justify-between px-6 py-4 border-b border-white/5">
        <span className="text-sm font-semibold text-teal tracking-wide">Portail Client</span>
        <button
          onClick={handleLogout}
          className="text-[11px] text-foreground-muted hover:text-error uppercase tracking-[0.2em] font-bold transition-opacity active:scale-95 opacity-60 hover:opacity-100"
          style={{ transition: 'opacity 180ms ease-out, transform 160ms ease-out' }}
        >
          Déconnexion
        </button>
      </header>
      <Routes>
        <Route index element={<Navigate to="/reservations/new" replace />} />
        <Route path="/reservations/*" element={<ReservationWizardShell />} />
        <Route path="*" element={<Navigate to="/" replace />} />
      </Routes>
    </main>
  )
}

function App() {
  return (
    <AppErrorBoundary appLabel="Le portail client">
      <AuthBootstrap>
        <BrowserRouter
          future={{
            v7_startTransition: true,
            v7_relativeSplatPath: true,
          }}
        >
          <Routes>
            <Route path="/login" element={<ClientLoginRoute />} />
            <Route element={<ProtectedClientShell />}>
              <Route path="/*" element={<ProtectedClientShell />} />
            </Route>
            <Route path="*" element={<Navigate to="/" replace />} />
          </Routes>
        </BrowserRouter>
      </AuthBootstrap>
    </AppErrorBoundary>
  )
}

export default App
```

DESIGN.md constraints applied:
- `transition: all` is FORBIDDEN — button uses explicit `transition: 'opacity 180ms ease-out, transform 160ms ease-out'`
- Colors use CSS var tokens (bg-background, text-teal, text-error) from theme.css

---

**File 2: app/frontend/portail/src/api/reservations.ts** — new file.

```typescript
import axiosInstance from '@shared/auth/axiosInstance'
import type { Table } from '@shared/types/tables'
import type { WizardState } from './pages/Reservations/WizardContext'

interface AvailableTablesParams {
  date: string
  heure_debut: string
  heure_fin: string
  nombre_personnes: number
}

export const fetchAvailableTables = async (params: AvailableTablesParams): Promise<Table[]> => {
  const { data } = await axiosInstance.get<Table[]>('/reservations/available_tables/', { params })
  return data
}

export const createReservation = async (state: WizardState) => {
  const payload = {
    table: state.selectedTable!.id,
    date_reservation: state.date,
    heure_debut: state.heure_debut + ':00',
    heure_fin: state.heure_fin + ':00',
    nombre_personnes: state.nombre_personnes,
  }
  const { data } = await axiosInstance.post('/reservations/', payload)
  return data
}
```

Note: `heure_debut + ':00'` converts the HTML time input value `'HH:MM'` to `'HH:MM:SS'` as required by DRF's TimeField. Never include `statut` in the payload — serializer returns 400 for non-staff users (CR-01).

---

**File 3: app/frontend/portail/src/pages/Reservations/WizardContext.tsx** — new file.

```typescript
import { createContext, useContext, useState } from 'react'
import type { ReactNode } from 'react'
import type { Table } from '@shared/types/tables'

export interface WizardState {
  date: string
  heure_debut: string
  heure_fin: string
  nombre_personnes: number
  selectedTable: Table | null
}

interface WizardContextValue {
  state: WizardState
  setDateTime: (fields: Pick<WizardState, 'date' | 'heure_debut' | 'heure_fin' | 'nombre_personnes'>) => void
  setTable: (table: Table) => void
  reset: () => void
}

const WizardContext = createContext<WizardContextValue | null>(null)

export const useWizard = (): WizardContextValue => {
  const ctx = useContext(WizardContext)
  if (!ctx) throw new Error('useWizard must be used inside WizardProvider')
  return ctx
}

const INITIAL_STATE: WizardState = {
  date: '',
  heure_debut: '',
  heure_fin: '',
  nombre_personnes: 1,
  selectedTable: null,
}

export const WizardProvider = ({ children }: { children: ReactNode }) => {
  const [state, setState] = useState<WizardState>(INITIAL_STATE)

  const setDateTime = (fields: Pick<WizardState, 'date' | 'heure_debut' | 'heure_fin' | 'nombre_personnes'>) =>
    setState((prev) => ({ ...prev, ...fields }))

  const setTable = (table: Table) =>
    setState((prev) => ({ ...prev, selectedTable: table }))

  const reset = () => setState(INITIAL_STATE)

  return (
    <WizardContext.Provider value={{ state, setDateTime, setTable, reset }}>
      {children}
    </WizardContext.Provider>
  )
}
```

---

**File 4: app/frontend/portail/src/pages/Reservations/WizardContext.test.tsx** — new file.

```typescript
import { render, renderHook, act } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import { WizardProvider, useWizard } from './WizardContext'

const wrapper = ({ children }: { children: React.ReactNode }) => (
  <WizardProvider>{children}</WizardProvider>
)

describe('WizardContext', () => {
  it('setDateTime updates date/time fields in state', () => {
    const { result } = renderHook(() => useWizard(), { wrapper })
    act(() => {
      result.current.setDateTime({
        date: '2026-06-15',
        heure_debut: '19:00',
        heure_fin: '21:00',
        nombre_personnes: 4,
      })
    })
    expect(result.current.state.date).toBe('2026-06-15')
    expect(result.current.state.heure_debut).toBe('19:00')
    expect(result.current.state.heure_fin).toBe('21:00')
    expect(result.current.state.nombre_personnes).toBe(4)
  })

  it('setTable updates selectedTable in state', () => {
    const { result } = renderHook(() => useWizard(), { wrapper })
    const fakeTable = { id: 5, numero: 5, capacite: 4, statut: 'LIBRE' as const, pos_x: 100, pos_y: 100, est_active: true, created_at: '', updated_at: '' }
    act(() => {
      result.current.setTable(fakeTable)
    })
    expect(result.current.state.selectedTable?.id).toBe(5)
  })

  it('reset clears state back to initial values', () => {
    const { result } = renderHook(() => useWizard(), { wrapper })
    act(() => {
      result.current.setDateTime({ date: '2026-06-15', heure_debut: '19:00', heure_fin: '21:00', nombre_personnes: 4 })
      result.current.reset()
    })
    expect(result.current.state.date).toBe('')
    expect(result.current.state.nombre_personnes).toBe(1)
    expect(result.current.state.selectedTable).toBeNull()
  })

  it('useWizard throws when called outside WizardProvider', () => {
    expect(() => renderHook(() => useWizard())).toThrow('useWizard must be used inside WizardProvider')
  })
})
```

---

**File 5: app/frontend/portail/src/pages/Reservations/ReservationWizardShell.tsx** — new file.

This is a stub shell — it renders a placeholder route structure. Step components (StepDateTime, StepTableSelect, StepConfirm) are created in Plan 03. For now, the shell uses lazy placeholder divs so the routing tree compiles without importing missing files.

```typescript
import { Routes, Route, Navigate } from 'react-router-dom'
import { WizardProvider } from './WizardContext'

const StepPlaceholder = ({ label }: { label: string }) => (
  <div className="flex items-center justify-center min-h-[60vh]">
    <p className="text-foreground-muted text-sm">{label}</p>
  </div>
)

export const ReservationWizardShell = () => (
  <WizardProvider>
    <Routes>
      <Route path="new" element={<StepPlaceholder label="Étape 1 — Date & heure" />} />
      <Route path="table" element={<StepPlaceholder label="Étape 2 — Choix de la table" />} />
      <Route path="confirm" element={<StepPlaceholder label="Étape 3 — Confirmation" />} />
      <Route index element={<Navigate to="new" replace />} />
    </Routes>
  </WizardProvider>
)
```

Plan 03 will replace the StepPlaceholder usages with real step components. This stub is intentional — it allows tsc -b to pass in Wave 1 without Plan 03's files existing yet.
  </action>
  <verify>
    <automated>cd app/frontend/portail ; npx vitest run src/pages/Reservations/WizardContext.test.tsx --reporter=verbose 2>&amp;1</automated>
  </verify>
  <done>
    - App.tsx contains `BrowserRouter` and `ReservationWizardShell`
    - App.tsx does NOT contain `transition: all` (DESIGN.md compliance)
    - `api/reservations.ts` contains `fetchAvailableTables` and `createReservation` and does NOT contain `statut` in the POST payload
    - WizardContext.tsx contains `WizardProvider` and `useWizard` with TypeScript interfaces
    - WizardContext.test.tsx: 4 tests pass (setDateTime, setTable, reset, throws-outside-provider)
    - `cd app/frontend/portail ; npx tsc -b` exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Installed react-router-dom@6.30.3 and framer-motion@12.38.0 into portail. Wired BrowserRouter routing into App.tsx. Created WizardContext, API service module, and ReservationWizardShell stub. 4 WizardContext unit tests pass.
  </what-built>
  <how-to-verify>
    1. Run: `cd app/frontend/portail ; npx tsc -b` — expect: exits 0, zero TypeScript errors
    2. Run: `cd app/frontend/portail ; npx vitest run --reporter=verbose` — expect: 4 tests passed, 0 failed
    3. Run: `cd app/frontend/portail ; npm run dev` (optional) — expect: dev server starts on port 3003, navigate to http://localhost:3003/ and confirm redirect to /login or /reservations/new
  </how-to-verify>
  <resume-signal>Type "approved" or describe any TypeScript errors or test failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→API | axiosInstance injects Bearer token on every request; 401 triggers refresh interceptor |
| route guard | ProtectedClientShell redirects to /login if not authenticated or non-CLIENT role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Elevation of Privilege | ProtectedClientShell route guard | mitigate | `isRoleAllowed(user.role, CLIENT_ROLES)` check redirects non-client roles to /login; matches backoffice pattern |
| T-24-07 | Information Disclosure | WizardState in React context | accept | Wizard state is ephemeral browser memory (no localStorage); tab close clears it; no PII beyond booking details already known to the client |
| T-24-08 | Tampering | `heure_debut + ':00'` string concat in createReservation | mitigate | Native `<input type="time">` enforces HH:MM format; backend TimeField validates HH:MM:SS; malformed values return 400 |
</threat_model>

<verification>
Full portail build and test verification:

```powershell
cd app/frontend/portail ; npx tsc -b 2>&1
cd app/frontend/portail ; npx vitest run --reporter=verbose 2>&1
```

Expected: tsc exits 0, vitest shows 4 passed.
</verification>

<success_criteria>
- `package.json` contains `react-router-dom`, `framer-motion`, `vitest` in appropriate dep sections
- `vitest.config.ts` exists with `environment: 'jsdom'` and `setupFiles: './src/test/setup.ts'`
- `src/test/setup.ts` exists with `import '@testing-library/jest-dom'`
- App.tsx contains `BrowserRouter` with `v7_startTransition` and `v7_relativeSplatPath` future flags
- WizardContext.tsx exports `WizardProvider`, `useWizard`, `WizardState`
- api/reservations.ts exports `fetchAvailableTables` and `createReservation`; payload has no `statut`
- ReservationWizardShell.tsx exports `ReservationWizardShell` wrapping `WizardProvider`
- `npx tsc -b` exits 0
- `npx vitest run` shows 4 tests passing (WizardContext suite)
</success_criteria>

<output>
After completion, create `.planning/phases/24-reservations-client-ui/24-02-SUMMARY.md`
</output>
