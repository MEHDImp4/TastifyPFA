---
phase: 10-commandes-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
    "backend/tastify_backend/settings/base.py",
    "backend/apps/commandes/apps.py",
    "backend/apps/commandes/models.py",
    "backend/apps/commandes/__init__.py",
    "backend/apps/commandes/tests/test_models.py"
]
autonomous: true
requirements: [COMM-01, COMM-02]

must_haves:
  truths:
    - "The 'commandes' app is registered in Django settings."
    - "The Commande model exists with the specified fields (table, serveur, statut, montant_total, est_active)."
    - "Commande uses soft-delete (est_active=False) instead of hard delete."
  artifacts:
    - path: "backend/apps/commandes/models.py"
      provides: "Commande model definition"
    - path: "backend/apps/commandes/apps.py"
      provides: "App configuration"
  key_links:
    - from: "backend/tastify_backend/settings/base.py"
      to: "apps.commandes"
      via: "INSTALLED_APPS"
---

<objective>
Scaffold the 'commandes' bounded context and implement the base Commande model with soft-delete patterns.

Purpose: Establish the structural foundation for order management.
Output: Registered Django app with Commande model and basic integrity tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-commandes-model/10-RESEARCH.md
@.planning/phases/10-commandes-model/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold commandes app and register in settings</name>
  <files>
    backend/apps/commandes/__init__.py,
    backend/apps/commandes/apps.py,
    backend/tastify_backend/settings/base.py
  </files>
  <action>
    - Create the directory `backend/apps/commandes/` and `backend/apps/commandes/tests/`.
    - Create `backend/apps/commandes/__init__.py`.
    - Create `backend/apps/commandes/apps.py` using the standard AppConfig pattern.
    - Add `'apps.commandes'` to `INSTALLED_APPS` in `backend/tastify_backend/settings/base.py` after `'apps.tables'`.
    - Per D-01: Ensure it's a new bounded context.
  </action>
  <verify>
    <automated>python backend/manage.py check</automated>
  </verify>
  <done>App created and registered without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Commande model and soft-delete logic</name>
  <files>
    backend/apps/commandes/models.py,
    backend/apps/commandes/tests/test_models.py
  </files>
  <action>
    - Implement `Commande` model in `models.py` per D-02 and D-03.
    - Fields: table (FK to Table, PROTECT), serveur (FK to User, SET_NULL, null=True), statut (TextChoices), montant_total (DecimalField), est_active (BooleanField).
    - Statut Choices: EN_COURS, EN_CUISINE, PRETE, PAYEE, ANNULEE.
    - Implement `CommandeQuerySet` and `CommandeManager` with `active()` filter (per Pattern 1).
    - Override `delete()` to implement soft-delete (sets `est_active=False`) per D-04 and Pattern 3.
    - Add `__str__` returning `Commande #{pk} — Table {table_id}`.
    - Add indexes for `[statut, created_at]` and `[table]`.
    - Write unit tests in `test_models.py` verifying soft-delete and the `active()` manager.
  </action>
  <verify>
    <automated>python backend/manage.py test apps.commandes.tests.test_models</automated>
  </verify>
  <done>Commande model implemented and verified with tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> Model | Ensuring data integrity through FK constraints (PROTECT/SET_NULL) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | Commande.delete | mitigate | Implement soft-delete to preserve order history for auditing. |
| T-10-02 | Tampering | Commande.table | mitigate | Use models.PROTECT to prevent deleting tables with active orders. |
</threat_model>

<verification>
Ensure Commande model exists and `delete()` does not remove the record from the database but sets `est_active` to `False`.
</verification>

<success_criteria>
1. `apps.commandes` is in `INSTALLED_APPS`.
2. `Commande` model defined with all required fields and methods.
3. Unit tests for soft-delete pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-commandes-model/10-01-SUMMARY.md`
</output>
