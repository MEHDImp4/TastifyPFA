---
phase: 10-commandes-model
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [
    "backend/apps/commandes/models.py",
    "backend/apps/commandes/tests/test_models.py"
]
autonomous: true
requirements: [COMM-03, COMM-04]

must_haves:
  truths:
    - "CommandeLigne model exists and connects to Commande and Plat."
    - "CommandeLigne snapshots the unit price from Plat when saved."
    - "Database migrations are successfully applied."
  artifacts:
    - path: "backend/apps/commandes/migrations/0001_initial.py"
      provides: "Database schema for orders"
  key_links:
    - from: "CommandeLigne"
      to: "Plat"
      via: "prix_unitaire snapshot"
---

<objective>
Implement the CommandeLigne model with price snapshotting and execute the initial migration for the app.

Purpose: Enable adding items to orders with historical price preservation.
Output: CommandeLigne model and updated database schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-commandes-model/10-RESEARCH.md
@.planning/phases/10-commandes-model/10-PATTERNS.md
@.planning/phases/10-commandes-model/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CommandeLigne model</name>
  <files>
    backend/apps/commandes/models.py,
    backend/apps/commandes/tests/test_models.py
  </files>
  <action>
    - Implement `CommandeLigne` model in `models.py` per D-06, D-07.
    - Fields: commande (FK to Commande, CASCADE), plat (FK to Plat, PROTECT), quantite (PositiveIntegerField), prix_unitaire (DecimalField), statut (TextChoices), notes (TextField).
    - Statut Choices: EN_ATTENTE, EN_PREPARATION, PRET, SERVI, ANNULE.
    - Implement `__str__` as `{plat.nom} x{quantite}`.
    - Add index for `[commande, statut]`.
    - Override `save()` to snapshot `plat.prix` into `prix_unitaire` if not set (per D-08 and Pitfall 4).
    - Add unit tests in `test_models.py` for price snapshotting and FK integrity.
  </action>
  <verify>
    <automated>python backend/manage.py check</automated>
  </verify>
  <done>CommandeLigne model implemented and verified.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply migrations</name>
  <files>
    backend/apps/commandes/migrations/0001_initial.py
  </files>
  <action>
    - Run `python backend/manage.py makemigrations commandes`.
    - Verify that the migration has correct dependencies (menu, tables, users) as per RESEARCH.md.
    - Run `python backend/manage.py migrate commandes`.
  </action>
  <verify>
    <automated>python backend/manage.py migrate --check</automated>
  </verify>
  <done>Database schema updated with new models.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Model -> Database | Schema constraints ensuring valid quantity and non-null prices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | CommandeLigne.quantite | mitigate | Use PositiveIntegerField to prevent negative quantities. |
| T-10-04 | Tampering | CommandeLigne.prix_unitaire | mitigate | Snapshot price from Plat on server-side save to prevent client-side price manipulation. |
</threat_model>

<verification>
Check that CommandeLigne exists in DB and unit tests pass for price snapshotting.
</verification>

<success_criteria>
1. CommandeLigne model implemented.
2. Initial migration created and applied.
3. Tests for price snapshot pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-commandes-model/10-02-SUMMARY.md`
</output>
