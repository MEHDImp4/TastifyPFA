---
phase: 27-encaissement-ui
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - app/frontend/backoffice/src/components/salle/PaymentModal.tsx
  - app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx
autonomous: false
requirements: [ENC-01, ENC-02, ENC-03]
must_haves:
  truths:
    - "The Staff UI updates in real-time when a client completes a payment"
    - "Staff receives audio feedback on payment completion"
    - "The payment modal auto-closes or reflects 'Paid' state when session is resolved"
    - "Tables transition from OCCUPEE to LIBRE visually on the map after full payment"
  artifacts:
    - path: "app/frontend/backoffice/src/components/salle/PaymentModal.tsx"
      provides: "Real-time synced payment interface"
  key_links:
    - from: "Staff WebSocket"
      to: "PaymentModal.tsx"
      via: "staff_group message broadcast"
---

<objective>
Finalize the Encaissement UI by integrating real-time WebSocket updates, audio/visual feedback, and performing end-to-end verification of the full payment cycle (from QR generation to table release).

Purpose: Ensure a seamless, reactive experience for both staff and clients.
Output: Reactive payment UI and verified E2E flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/27-encaissement-ui/27-01-SUMMARY.md
@.planning/phases/27-encaissement-ui/27-02-SUMMARY.md
@app/frontend/shared/websocket/useStaffSocket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Real-time WebSocket Sync</name>
  <files>app/frontend/backoffice/src/components/salle/PaymentModal.tsx</files>
  <action>
    - Subscribe `PaymentModal` to WebSocket events (using `useStaffSocket` or similar).
    - Listen for `order_updated` or `payment_confirmed` events related to the current order.
    - When a payment is detected, refresh the `montant_restant` or close the modal if fully paid.
    - Ensure the modal reflects partial payments in real-time if multiple clients are splitting.
  </action>
  <verify>
    <automated>grep -q "useStaffSocket" app/frontend/backoffice/src/components/salle/PaymentModal.tsx</automated>
  </verify>
  <done>PaymentModal is reactive to backend signals.</done>
</task>

<task type="auto">
  <name>Task 2: Add Audio & Visual Feedback</name>
  <files>app/frontend/backoffice/src/components/salle/PaymentModal.tsx, app/frontend/backoffice/src/pages/Staff/Map/MapView.tsx</files>
  <action>
    - Integrate success audio chime on full payment (use existing `StaffNotificationManager` if available, or `@shared/media/success.mp3`).
    - Add a visual success state (e.g., Confetti or Green Checkmark) when an order is fully paid.
    - Ensure the table on the `MapView` updates its visual state (OCCUPEE -> LIBRE) immediately without page refresh.
  </action>
  <verify>
    <automated>grep -q "audio" app/frontend/backoffice/src/components/salle/PaymentModal.tsx</automated>
  </verify>
  <done>UI provides rich feedback on payment success.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full end-to-end payment and split-bill flow.</what-built>
  <how-to-verify>
    1. Open Staff UI and Client UI in separate windows.
    2. Occupy a table and create an order in Staff UI.
    3. Open Payment Modal on Staff UI and Generate QR.
    4. Copy the link/token and open it in the Client UI window.
    5. Perform an "Item Split" in Client UI and pay for one item.
    6. Observe Staff UI updating the remaining amount in real-time.
    7. Complete the rest of the payment in Client UI.
    8. Verify Staff UI plays success chime, modal closes, and table becomes LIBRE on the map.
  </how-to-verify>
  <resume-signal>Type "approved" if the E2E flow is reactive and accurate.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WebSocket -> UI | Real-time payment state updates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Tampering | WS Event Injection | accept | UI state is secondary; backend is source of truth for all transactions. |
</threat_model>

<verification>
Perform the human-verify checkpoint steps.
</verification>

<success_criteria>
- Zero manual refreshes required to see payment progress.
- Audio feedback functions correctly.
- Table map accurately reflects session completion.
</success_criteria>

<output>
After completion, create `.planning/phases/27-encaissement-ui/27-03-SUMMARY.md`
</output>
