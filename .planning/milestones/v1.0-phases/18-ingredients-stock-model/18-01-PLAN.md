---
phase: "18-ingredients-stock-model"
plan: "18-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "app/backend/tastify_backend/settings.py"
  - "app/backend/apps/stock/models.py"
  - "app/backend/apps/menu/models.py"
  - "app/backend/apps/stock/tests/test_models.py"
autonomous: true
requirements: ["REQ-02", "REQ-03"]
must_haves:
  truths:
    - "Plats can be linked to multiple ingredients with specific required quantities."
    - "Ingredients support soft-delete (`est_active`)."
    - "Strict base units are used in the DB."
  artifacts:
    - path: "app/backend/apps/stock/models.py"
      provides: "Ingredient and PlatIngredient models"
    - path: "app/backend/apps/menu/models.py"
      provides: "Plat model updated with M2M"
  key_links:
    - from: "app/backend/apps/menu/models.py"
      to: "app/backend/apps/stock/models.py"
      via: "ManyToManyField with through='stock.PlatIngredient'"
---

<objective>
Create the `stock` Django app, define the `Ingredient` and `PlatIngredient` models, and update the existing `Plat` model with a Many-To-Many relationship. Establish the TDD testing foundation for models.

Purpose: Implement the core database models for Ingredients and Stock tracking.
Output: The `stock` Django app with updated models, tested and migrated.
</objective>

<context>
@.planning/phases/18-ingredients-stock-model/18-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create stock app and models</name>
  <files>app/backend/tastify_backend/settings.py, app/backend/apps/stock/models.py, app/backend/apps/menu/models.py</files>
  <action>
    1. Create the `stock` app in `app/backend/apps/stock` and add it to `INSTALLED_APPS` in `settings.py` (or equivalent core settings).
    2. Define the `Ingredient` model in `apps/stock/models.py` with fields: `nom` (unique), `unite_mesure` (choices: g, ml, pcs), `stock_actuel` (Decimal), `seuil_alerte` (Decimal), `est_active` (Bool), timestamps. Add a custom `delete()` method for soft deletion.
    3. Define the `PlatIngredient` model in `apps/stock/models.py` with foreign keys to `menu.Plat` and `Ingredient`, and `quantite_requise` (Decimal). Enforce `unique_together` on the plat and ingredient.
    4. Update the `Plat` model in `apps/menu/models.py` to include `ingredients = models.ManyToManyField('stock.Ingredient', through='stock.PlatIngredient', related_name='plats')`. (per D-01)
    5. DO NOT implement unit conversions in the models. Use strict base units. (per D-03, D-04)
    6. Run migrations creation to capture these changes.
  </action>
  <verify>
    <automated>docker compose exec backend python manage.py makemigrations --dry-run</automated>
  </verify>
  <done>Models defined and migrations created successfully without pending changes.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for models</name>
  <files>app/backend/apps/stock/tests/test_models.py</files>
  <action>
    Write tests in `apps/stock/tests/test_models.py` covering:
    - `Ingredient` model creation and soft delete behavior.
    - `PlatIngredient` M2M linking and `unique_together` constraints.
  </action>
  <verify>
    <automated>docker compose exec backend pytest apps/stock/tests/test_models.py -x</automated>
  </verify>
  <done>All tests pass confirming soft-delete, M2M relations, and base unit properties.</done>
</task>
</tasks>
