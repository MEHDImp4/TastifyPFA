# Plan 11-03: Custom Actions & Final Verification

## Goal
Implement refinement features like adding items to existing orders and perform final API verification.

## Proposed Changes

### backend/apps/commandes/views.py
- Add `@action(detail=True, methods=['post']) add_items`.
  - Validates `CommandeLigne` data.
  - Creates lines linked to current order.
  - Returns updated order representation.
- Implement `destroy()` for soft-delete.

### backend/apps/commandes/tests/test_api_advanced.py
- Test `add_items` action.
- Test soft-delete behavior.
- Test complex order lifecycle (Create -> Add Items -> Pay -> Check Table Status).

## Verification Plan

### Automated Tests
- `python backend/manage.py test apps.commandes`
