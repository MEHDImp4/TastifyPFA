---
phase: 12-order-taking-frontend
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified: [
  "frontend/salle/src/pages/Ordering/components/CategoryTabs.tsx",
  "frontend/salle/src/pages/Ordering/components/DishCard.tsx",
  "frontend/salle/src/pages/Ordering/components/DishGrid.tsx",
  "frontend/salle/src/pages/Ordering/components/DishGrid.test.tsx",
  "frontend/salle/src/pages/Ordering/OrderingPage.tsx"
]
autonomous: true
requirements: [ORD-FE-02, ORD-FE-03]

must_haves:
  truths:
    - "Horizontal category tabs allow switching between menu categories"
    - "Dish grid updates based on selected category"
    - "Dish cards show price, image, and quantity controls"
    - "Clicking +/- on a dish card updates the Zustand store"
  artifacts:
    - path: "frontend/salle/src/pages/Ordering/components/CategoryTabs.tsx"
      provides: "Scroll-snap horizontal navigation"
    - path: "frontend/salle/src/pages/Ordering/components/DishCard.tsx"
      provides: "Interactive item component"
    - path: "frontend/salle/src/pages/Ordering/components/DishGrid.test.tsx"
      provides: "Tests for category filtering and dish rendering"
  key_links:
    - from: "OrderingPage.tsx"
      to: "/api/categories/"
      via: "fetch/axios in useEffect"
    - from: "OrderingPage.tsx"
      to: "/api/plats/"
      via: "fetch/axios in useEffect"
---

<objective>
Implement the menu browsing experience with category filtering and dish selection.

Purpose: Allow servers to quickly find and add items to a table's cart.
Output: Functional category tabs, a responsive dish grid, interactive dish cards, and filtering tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-order-taking-frontend/12-RESEARCH.md
@DESIGN.md
@frontend/salle/src/pages/Ordering/OrderingPage.tsx
@frontend/salle/src/pages/Ordering/store/useOrderStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement CategoryTabs with CSS Scroll Snapping</name>
  <files>frontend/salle/src/pages/Ordering/components/CategoryTabs.tsx</files>
  <action>
    - Create `CategoryTabs.tsx` using the implementation pattern from RESEARCH.md.
    - Use `snap-x snap-proximity` and `flex-none` for horizontal scrolling.
    - Use Teal `#2A9D8F` for the active tab background.
    - Apply `active:scale-97` for tactile feedback on tab clicks.
    - Add `overscroll-behavior-x: contain` to prevent browser navigation triggers.
  </action>
  <verify>
    <automated>grep "#2A9D8F" frontend/salle/src/pages/Ordering/components/CategoryTabs.tsx</automated>
  </verify>
  <done>Category tabs component created with correct CSS and design system tokens</done>
</task>

<task type="auto">
  <name>Task 2: Implement DishCard with quantity controls</name>
  <files>frontend/salle/src/pages/Ordering/components/DishCard.tsx</files>
  <action>
    - Create `DishCard.tsx` following DESIGN.md (Ardoise background `#264653`, 1px border `white/10`).
    - Use `active:scale-97` for the card or buttons.
    - Use Teal `#2A9D8F` for the "Add" or "+" buttons.
    - If needed, use Terracotta/Coral `#E76F51` for "Remove" or destructive actions.
    - Display image (with fallback), name, and price.
    - Connect to `useOrderStore` to trigger `addItem` and `removeItem`.
  </action>
  <verify>
    <automated>grep "active:scale-97" frontend/salle/src/pages/Ordering/components/DishCard.tsx</automated>
  </verify>
  <done>DishCard can trigger store updates and follows design interaction mandates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement DishGrid with filtering and tests</name>
  <files>frontend/salle/src/pages/Ordering/components/DishGrid.tsx, frontend/salle/src/pages/Ordering/components/DishGrid.test.tsx, frontend/salle/src/pages/Ordering/OrderingPage.tsx</files>
  <behavior>
    - `DishGrid` renders only dishes belonging to the `selectedCategoryId`.
    - `DishGrid` shows all dishes if `selectedCategoryId` is null/undefined (if applicable).
    - `DishGrid` uses a responsive grid layout.
  </behavior>
  <action>
    - Create `DishGrid.test.tsx` to verify filtering logic.
    - In `DishGrid.tsx`:
      - Render filtered dishes based on `selectedCategoryId`.
      - Use `framer-motion` `layout` prop for smooth transitions.
    - In `OrderingPage.tsx`:
      - Fetch categories and dishes from API.
      - Manage `selectedCategoryId` state.
  </action>
  <verify>
    <automated>cd frontend/salle; npm test -- DishGrid.test.tsx</automated>
  </verify>
  <done>Menu browser is functional with filtering, verified by TDD tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> UI | Data from `/api/plats/` must be typed correctly |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | I | DishGrid | mitigate | Ensure only available dishes are displayed (API filtering + UI check) |
</threat_model>

<verification>
- Verify that horizontal scrolling works on the tabs.
- Verify that clicking a category updates the dish list.
- Verify that adding an item updates the card quantity UI.
- Ensure `DishGrid.test.tsx` passes.
</verification>

<success_criteria>
- Seamless category switching with Teal highlights.
- Responsive and tactile dish selection experience with scale feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/12-order-taking-frontend/12-02-SUMMARY.md`
</output>
