---
phase: 03
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [
  frontend/back-office/src/App.tsx,
  frontend/salle/src/App.tsx,
  frontend/kds/src/App.tsx,
  frontend/portail-client/src/App.tsx
]
autonomous: false
requirements: [AUTH-04, AUTH-05]
must_haves:
  truths:
    - "Unauthenticated users are redirected to the Login page in all 4 SPAs"
    - "Successful login grants access to the main application view"
    - "Logout redirects users back to the Login page"
    - "Auth state persists during the session (memory-based)"
  artifacts:
    - path: "frontend/back-office/src/App.tsx"
      provides: "Login integration for Back-office"
    - path: "frontend/salle/src/App.tsx"
      provides: "Login integration for Salle"
  key_links:
    - from: "frontend/back-office/src/App.tsx"
      to: "@shared/auth/Login"
      via: "import and render"
---

<objective>
Integrate the shared Login component and authentication state management into all four React SPAs (Back-office, Salle, KDS, and Portail Client). Ensure proper redirection and route guarding based on authentication status.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-auth-api-login/03-01-SUMMARY.md
@.planning/phases/03-auth-api-login/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Login in Back-office and Salle SPAs</name>
  <files>frontend/back-office/src/App.tsx, frontend/salle/src/App.tsx</files>
  <action>
    - Import `Login` from `@shared/auth/Login`.
    - Import `useAuthStore` from `@shared/auth/useAuthStore`.
    - Modify `App.tsx` to check `isAuthenticated`:
      - If NOT authenticated: Render `<Login />`.
      - If authenticated: Render the existing app content.
    - Add a "Logout" button/link to verify the logout flow.
  </action>
  <verify>
    <automated>grep "Login" frontend/back-office/src/App.tsx</automated>
  </verify>
  <done>Login integrated into Back-office and Salle apps.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Login in KDS and Portail Client SPAs</name>
  <files>frontend/kds/src/App.tsx, frontend/portail-client/src/App.tsx</files>
  <action>
    - Repeat the integration steps for KDS and Portail Client apps.
    - Ensure `isAuthenticated` logic correctly guards the main content.
  </action>
  <verify>
    <automated>grep "Login" frontend/kds/src/App.tsx</automated>
  </verify>
  <done>Login integrated into KDS and Portail Client apps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authentication flow integrated across all 4 React SPAs.</what-built>
  <how-to-verify>
    1. Start the project: `docker compose up -d`.
    2. Visit each SPA:
       - Back-office: http://localhost:3000 (proxied via nginx or direct)
       - Salle: http://localhost:3001
       - KDS: http://localhost:3002
       - Portail: http://localhost:3003
    3. Verify that each app shows the Login screen.
    4. Log in using a seeded user (e.g., admin/admin if seeded, or create via django shell).
    5. Verify successful entry into the app.
    6. Verify logout functionality.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<success_criteria>
1. All 4 SPAs require authentication to see main content.
2. Login/Logout flows work seamlessly across all apps.
3. Access tokens are used in requests (verifiable via browser Network tab).
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-api-login/03-03-SUMMARY.md`
</output>
