---
phase: 05-categories-frontend
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - frontend/back-office/src/components/ui/Switch.tsx
  - frontend/back-office/src/components/ui/Switch.test.tsx
  - frontend/back-office/src/components/ui/Drawer.tsx
  - frontend/back-office/src/components/ui/Drawer.test.tsx
  - frontend/back-office/src/pages/Categories/CategoryRow.tsx
  - frontend/back-office/src/pages/Categories/CategoryRow.test.tsx
  - frontend/back-office/src/pages/Categories/CategoryList.tsx
  - frontend/back-office/src/pages/Categories/CategoryDrawer.tsx
  - frontend/back-office/src/pages/Categories/CategoryDrawer.test.tsx
  - frontend/back-office/src/pages/Categories/index.tsx
autonomous: true
requirements: [PHASE-05-2]
must_haves:
  truths:
    - Manager can view a list of categories
    - Manager can toggle active state of a category directly from the list
    - Manager can create and edit a category via sliding drawer
    - Manager can delete a category via inline confirmation
  artifacts:
    - path: frontend/back-office/src/pages/Categories/CategoryList.tsx
      provides: Data table component for categories
    - path: frontend/back-office/src/pages/Categories/CategoryDrawer.tsx
      provides: Create/Edit slide-over form for categories
  key_links:
    - from: frontend/back-office/src/pages/Categories/CategoryRow.tsx
      to: frontend/_shared/auth/axiosInstance.ts
      via: axios patch and delete API calls
    - from: frontend/back-office/src/pages/Categories/CategoryDrawer.tsx
      to: frontend/_shared/auth/axiosInstance.ts
      via: axios post and put/patch API calls for create/update
---

<objective>
Implement the core CRUD views for managing categories in the back-office with Test-Driven Delivery.
Purpose: Allow GERANT role users to list, create, edit, activate/deactivate, and soft-delete menu categories.
Output: Working data table and sliding drawer form connected to the Django REST API, fully tested.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-categories-frontend/05-CONTEXT.md
</context>

<interfaces>
From backend API (verified in Phase 04):
`Categorie` fields: `id`, `nom`, `description`, `ordre_affichage`, `image` (URL), `est_active`, `created_at`, `updated_at`.
Endpoints: `GET /api/categories/`, `POST /api/categories/`, `PATCH /api/categories/{id}/`, `DELETE /api/categories/{id}/`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Reusable UI Primitives (Switch & Drawer)</name>
  <files>frontend/back-office/src/components/ui/Switch.tsx, frontend/back-office/src/components/ui/Switch.test.tsx, frontend/back-office/src/components/ui/Drawer.tsx, frontend/back-office/src/components/ui/Drawer.test.tsx</files>
  <read_first>frontend/_shared/theme.css, .planning/phases/05-categories-frontend/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Switch updates visuals smoothly based on checked state
    - Test 2: Drawer renders content over an overlay when isOpen is true
  </behavior>
  <action>
    Create `frontend/back-office/src/components/ui/Switch.tsx` as a controlled toggle component (`<button onClick={onToggle}>`) using Tailwind utilities `bg-teal-500` for active state, `bg-surface-elevated` for inactive state, animating an inner white circle with `translate-x-5` / `translate-x-1`.
    Create `frontend/back-office/src/components/ui/Drawer.tsx` rendering a fixed panel (e.g., `fixed inset-y-0 right-0 w-[400px] bg-surface shadow-2xl p-6 z-50 overflow-y-auto`) when `isOpen` is true. Add a semi-transparent overlay behind it (`fixed inset-0 bg-black/50 z-40`) that triggers `onClose` when clicked. Apply the predefined `.animate-enter` class to the drawer panel for smooth transitions.
  </action>
  <verify>
    <automated>npm test --prefix frontend/back-office</automated>
  </verify>
  <acceptance_criteria>
    - Switch updates visuals smoothly based on boolean checked prop.
    - Drawer component accepts `isOpen`, `onClose`, and `children` props, rendering its content over an overlay.
  </acceptance_criteria>
  <done>Core layout primitives created and type-checked successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Category List and Row Components</name>
  <files>frontend/back-office/src/pages/Categories/CategoryRow.tsx, frontend/back-office/src/pages/Categories/CategoryList.tsx, frontend/back-office/src/pages/Categories/CategoryRow.test.tsx</files>
  <read_first>frontend/_shared/auth/axiosInstance.ts</read_first>
  <behavior>
    - Test 1: Row renders category details and switch
    - Test 2: Row shows confirmation on delete click and calls delete API
    - Test 3: Switch click calls patch API
  </behavior>
  <action>
    Create `frontend/back-office/src/pages/Categories/CategoryRow.tsx`. Props: `category` (object), `onEdit(category)`, `onRefresh()`.
    - Setup an `isConfirming` state. If false, show Edit (Lucide `Pencil`) and Delete (Lucide `Trash2`) buttons.
    - If Delete is clicked, set `isConfirming=true`. Show "Confirmer" (red text) and "Annuler" buttons. If "Confirmer" is clicked, call `axiosInstance.delete('/api/categories/${category.id}/')` and call `onRefresh()`.
    - Auto-revert `isConfirming` to false after 3 seconds using `setTimeout` in a `useEffect`.
    - Include `<Switch>` to instantly update `est_active` by sending `PATCH` on click. If `est_active` is false, apply `opacity-50` class to the table row (`<tr>`).
    Create `frontend/back-office/src/pages/Categories/CategoryList.tsx` rendering a standard HTML `<table>` (with `thead` containing: Image, Nom, Ordre, Actif, Actions). Map over a `categories` prop array to render `<CategoryRow>` components.
  </action>
  <verify>
    <automated>npm test --prefix frontend/back-office</automated>
  </verify>
  <acceptance_criteria>
    - Row handles an inline delete confirmation flow without modals.
    - Row opacity correctly dims for inactive items (est_active).
    - API soft-deletes and status updates are wired to axiosInstance.
    - Tests verify row interaction and API calls.
  </acceptance_criteria>
  <done>Table component accurately reflects state and processes user actions inline with full test coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build Drawer Form and Connect Data Fetching</name>
  <files>frontend/back-office/src/pages/Categories/CategoryDrawer.tsx, frontend/back-office/src/pages/Categories/index.tsx, frontend/back-office/src/pages/Categories/CategoryDrawer.test.tsx</files>
  <read_first>frontend/_shared/auth/axiosInstance.ts, .planning/phases/05-categories-frontend/05-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Drawer renders form with inputs
    - Test 2: Form submission requires 'nom' field (validation works)
    - Test 3: Form shows image preview for new image or existing URL
    - Test 4: Form submits via FormData
  </behavior>
  <action>
    Create `frontend/back-office/src/pages/Categories/CategoryDrawer.tsx` rendering inside `<Drawer>`. Form inputs should include `nom`, `description`, `ordre_affichage` (number), and `image` (`type="file"`, `accept="image/*"`). 
    Per D-12, explicitly implement required validation on the `nom` field: ensure the field is not empty before allowing submission. If empty, prevent submit and show a validation error.
    Per D-10, implement the image preview logic: display a small square preview (≈80px) of the selected image before submitting. When editing, show the existing image URL as the current preview.
    Pre-fill inputs using `initialData` object if editing. Form `onSubmit` MUST use `FormData` (not JSON stringify) to support multipart image uploads. Call `POST /api/categories/` for new category, `PUT /api/categories/${id}/` or `PATCH` for edits using `axiosInstance`. Execute `onSuccess` callback post-completion.
    Update `frontend/back-office/src/pages/Categories/index.tsx` to handle top-level page state: `categories` (array), `isDrawerOpen` (boolean), `editingCategory` (object | null). 
    - Use `useEffect` with `axiosInstance.get('/api/categories/')` to populate `categories` array on mount. 
    - Render a "Nouvelle Catégorie" button at top-right to open empty drawer.
    - Render `<CategoryList>` (pass `categories`, `onEdit={() => ...}`, `onRefresh={fetchData}`) and `<CategoryDrawer>` (pass `isOpen`, `onClose`, `initialData`, `onSuccess={fetchData}`).
  </action>
  <verify>
    <automated>npm test --prefix frontend/back-office</automated>
  </verify>
  <acceptance_criteria>
    - Drawer submits data using native `FormData` format to handle image attachments safely.
    - Image preview displays properly for both new file selections and existing image URLs (D-10).
    - Basic non-empty validation on the `nom` field is enforced (D-12).
    - Categories index.tsx automatically fetches and dynamically renders HTTP API response arrays.
    - Tests verify form validation, preview logic, and submit.
  </acceptance_criteria>
  <done>Full CRUD integration complete and tested for categories frontend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form→API | Form data validation should be backed by API strictness, but handled gracefully on frontend to prevent bad submissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | CategoryDrawer | mitigate | Frontend ensures multipart form data is assembled properly. API handles final injection checks for actual file type. |
| T-05-02-02 | Elevation | CategoryRow | mitigate | Fallback to API rejection: Ensure only GERANT users execute successful writes despite visual state. |
</threat_model>

<verification>
Ensure data correctly saves and refreshes in the list without full page reloads, and image uploads securely display after submitting the drawer form. Tests must pass.
</verification>

<success_criteria>
Categories CRUD works end-to-end via the React UI. Toggles send proper PATCH requests, delete uses 3-second inline confirmation, drawer handles image uploads explicitly with FormData objects, image previews work correctly, and basic validation blocks empty fields. Tests cover all interaction logic.
</success_criteria>

<output>
After completion, create `.planning/phases/05-categories-frontend/05-02-SUMMARY.md`
</output>